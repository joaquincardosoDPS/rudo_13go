# CLAUDE.md — reglas de trabajo en este proyecto

## Qué es esto

Canal Roku (BrightScript/SceneGraph) para el cliente **13go** (Canal 13, Chile), construido como copia y aprendizaje a partir de [`../roku-chv`](../roku-chv) (cliente MiCHV). El dueño del proyecto está aprendiendo Roku/BrightScript desde cero mientras lo construye.

## Cómo trabajar en este proyecto (importante, no lo ignores)

- **Aplicá los cambios vos mismo, sin pedir confirmación.** El usuario autorizó editar directamente los archivos de código (BrightScript, XML, JSON de configuración). No preguntes "¿lo aplico?".
- **Después de cada cambio de código (obligatorio):** verificá que compile con `brs-node` (ver "Comandos") y regenerá `canal13go.zip` en la raíz.
- Tareas puramente mecánicas que el usuario pida directamente (copiar carpetas, `git init`, mover archivos) sí se pueden ejecutar directamente, no tienen valor de aprendizaje.
- Ve paso a paso. No te adelantes a fases futuras del plan sin que el usuario lo pida — aunque tengas contexto para hacerlo, la idea es que él controle el ritmo.
- Actualiza la sección "Estado actual" de este archivo a medida que se completen pasos, para que cualquier sesión futura (tuya o de otra persona) sepa exactamente dónde retomar sin releer todo el historial.

## Contexto del backend de 13go (clave — no asumir que es igual a MiCHV)

- MiCHV (`roku-chv`) usa REST directo en `consumers.rudo.video/{recurso}` con `client=chv` para catálogo, perfiles y auth.
- **13go usa un patrón distinto:**
  - Catálogo (programas/categorías/videos): feed tipo WordPress en `www.13.cl/13go-premium/feed/...`.
  - Perfiles: proxy a Firebase en `https://rudo.video/gateway/13go/` — POST con `FormData` y campos `action=firebase`, `path=...`, `token=...`.
  - Referencia real de estos endpoints: proyecto React `c13_reloaded` en `C:\Users\Joaquin\Documents\projects\c13_reloaded` (ver `src/config-global.ts` y `src/services/*.ts`).
- Streaming/EPG/playlists sí comparten el CDN de rudo.video, pero bajo el slug de tenant `canal-13` (no `13go`) — ej. `cdn.rudo.video/assets/canal-13/playlists/...`.
- **Decisión tomada (2026-09-03):** replicar exactamente el comportamiento de `c13_reloaded`, no reutilizar el patrón REST de MiCHV. Esto implica reescribir `source/apis/ContentAPI.brs` y `source/apis/BaseRequests.brs` más adelante, no solo cambiar `AppConfig.json`.

## Proyecto de referencia: `c13_reloaded`

`c13_reloaded` es la app web (React + Vite + TS) de 13go **ya funcional en producción** — es la fuente de verdad de negocio/backend/marca para este puerto a Roku. Ruta completa:

```
C:\Users\Joaquin\Documents\projects\c13_reloaded
```

No hay que "buscarlo" cada vez — este es el mapa de las rutas que ya importan dentro de ese proyecto:

```
c13_reloaded/
├── config.json                        # secciones/orden de la home (tipo "destacados", "categoria_carrusel", etc.)
├── src/
│   ├── config-global.ts               # ⭐ TODAS las URLs base (feed 13.cl, rudo CDN, gateway Firebase, access token)
│   ├── App.tsx / main.tsx             # bootstrap de la app (equivalente conceptual a Main() + MainScene)
│   ├── router/
│   │   ├── index.tsx                  # definición de rutas = equivalente a las "páginas" del ViewStackManager
│   │   └── config.tsx
│   ├── layout/
│   │   ├── MainLayout.tsx             # shell con sidebar = equivalente a MainScene + TopMenu
│   │   ├── AuthGuard.tsx              # lógica de "¿está logueado?" = equivalente a isUserLoggedIn en MainScene
│   │   └── components/Sidebar.tsx     # menú lateral = equivalente a TopMenu
│   ├── pages/                         # ⭐ una carpeta por pantalla — mapea 1:1 a components/pages/* en el Roku
│   │   ├── Splash/SplashView.tsx      # → OnboardingPage
│   │   ├── WhosThere/                 # → EditorProfilesPage (selección de perfil)
│   │   ├── Auth/
│   │   │   ├── ConnectView.tsx        # → DeviceLinkPage (vincular TV / QR)
│   │   │   ├── LoginView.tsx          # → LoginPage
│   │   │   ├── RegisterView.tsx       # → SignUpPage
│   │   │   └── SuscribeView.tsx
│   │   ├── Home/HomeView.tsx          # → HomePage
│   │   ├── Live/LiveView.tsx          # → LivePage
│   │   ├── Radio/RadioView.tsx        # (no existe en MiCHV — feature nueva a evaluar)
│   │   ├── Search/SearchView.tsx      # → SearchPage
│   │   ├── VOD/
│   │   │   ├── VODView.tsx            # → CategoryDetailPage / ProgramsPage
│   │   │   ├── Program/               # → DetailPage
│   │   │   └── Player/                # → VideoPlayer
│   │   └── Account/
│   │       ├── AccountView.tsx        # → AccountPage
│   │       └── EditProfileView.tsx    # → EditorProfilesPage (edición)
│   ├── services/                      # ⭐⭐ LA REFERENCIA para reescribir ContentAPI.brs/BaseRequests.brs
│   │   ├── rudo.ts                    # cliente base hacia rudo.video (streaming/EPG)
│   │   ├── canal13GoService.ts        # catálogo VOD: programas, categorías, capítulos (feed 13.cl)
│   │   ├── homeSectionsService.ts     # arma las filas de la home según config.json
│   │   ├── searchService.ts           # búsqueda
│   │   ├── favoriteService.ts         # favoritos / "mi lista"
│   │   ├── profileService.ts          # perfiles vía gateway Firebase (action=firebase)
│   │   ├── hlsSessionService.ts       # sesión de reproducción HLS
│   │   ├── deviceAdService.ts         # anuncios (VAST/VMAP) — cruza con roku_ads_lib/googleima3 del manifest
│   │   ├── ga4Service.ts / trackingService.ts  # analítica (evaluar si aplica igual en TV)
│   │   └── globlalService.ts          # (sic, typo en el nombre real del archivo) config global / bootstrap
│   ├── features/auth/                 # lógica de autenticación (context, hooks, services, types.ts)
│   ├── hooks/                         # use-home-data.ts, use-vod-data.ts, use-program-data.ts, etc. — piensa
│   │                                   # estos como "la versión React" de lo que en Roku son las funciones
│   │                                   # `On...APIResponse` en cada página (mismo rol: pedir datos y mapearlos a UI)
│   ├── interfaces/                    # tipos TS de las respuestas de API — úsalos como referencia de la forma
│   │                                   # real del JSON al escribir los parsers .brs (ej. HomePageParser.brs)
│   └── assets/
│       ├── fonts/                     # DMSans-*.ttf (usar estos en el Roku), CircularXX-*.woff2 (solo web)
│       └── images/
│           ├── logos/                 # 13-go-logo.png ⭐ logo oficial
│           ├── qr/                    # qr-13-go-tv-rounded.png, qr-13go-tv.png, etc.
│           ├── backgrounds/           # bg-start.png, bg-linear-gradient-main.png
│           └── buttons/ , iconos/     # iconografía de referencia (formato SVG/PNG, hay que exportar a Roku)
└── apps/tizen, apps/webos             # builds empaquetados para otras TVs — no relevantes para el puerto Roku
```

**Regla práctica:** cuando lleguemos a reescribir una función de `ContentAPI.brs`, el primer lugar donde buscar el endpoint/forma de los datos es el archivo de `src/services/*.ts` equivalente, no adivinar.

## Assets de marca reales (extraídos de `c13_reloaded/src/assets`)

- Logo: `images/logos/13-go-logo.png`.
- Color primario: `#FA6428` (naranjo). Fondos oscuros: `#08090C` / `#060608` / `#1C1D28`. Acento: amarillo `#ffcf04`.
- Tipografía: **DMSans** (ttf, varios pesos) en `assets/fonts/`.
- Íconos/splash con tamaño exacto que exige Roku: **pendientes**. No asumas medidas de memoria — verifica contra la documentación oficial de Roku antes de exportar los finales.

## Comandos

No hay `package.json` ni build system: este es un canal Roku puro (BrightScript + SceneGraph XML), sin transpilación ni bundler.

- **Formato de código:** [bsfmt.json](bsfmt.json) define el estilo (4 espacios, `LF`, `AND`/`OR` en mayúsculas, resto de keywords en su casing original) para el formateador `bsfmt` (paquete `brighterscript-formatter`) — si el usuario lo tiene instalado, se corre como `bsfmt --config bsfmt.json` sobre los `.brs` modificados. No hay linter (`bslint`/`.bsconfig`) configurado en el repo.
- **Empaquetar y probar en un Roku real:** no hay script propio en el repo; es el flujo estándar de sideload de Roku — comprimir el proyecto (manifest + carpetas en la raíz) en un zip y subirlo vía `http://<ip-del-roku>/plugin_install` (form `mysubmit=Install`, modo desarrollador activo en el dispositivo). El log en vivo se ve por telnet al puerto 8085 del Roku.
- **Pruebas automatizadas:** no existen (no hay carpeta `tests/`). Las carpetas [rasp/](rasp/) contienen scripts RASP (Roku Automated Screenshot Program) para certificación/QA manual guiada, no tests unitarios — y aún referencian el canal `MiCHV` (pendiente de actualizar a 13go cuando se llegue a esa fase).
- **Probar sin un Roku físico:** `brs-node` (motor de simulación BrightScript de la comunidad, [lvcabral/brs-engine](https://github.com/lvcabral/brs-engine)) corre el canal empaquetado en un `.zip` desde la terminal, con `print`/logs reales y sin las limitaciones de CORS del navegador (las llamadas de red sí funcionan, corren desde Node). Uso: `npm install brs-node` en un proyecto scratch, después `npx brs-cli --log-level debug ruta/al/canal.zip`. (Si `npx` deja de resolver los binarios locales y busca `brs-cli`/`bsc` en el registro de npm, correrlos directo: `node node_modules/brs-node/bin/brs.cli.js canal.zip` y `node node_modules/brighterscript/dist/cli.js`.) **Ojo al empaquetar el zip en Windows:** `Compress-Archive` de PowerShell guarda las rutas internas con `\` en vez de `/`, lo cual rompe la resolución de `pkg:/...` — hay que armar el zip con una herramienta que use `/` (ej. `adm-zip` de Node, o `zip` de Git Bash/WSL si está disponible).

## Arquitectura de alto nivel

- **Arranque:** [source/main.brs](source/main.brs) es el entry point BrightScript puro: crea el `roSGScreen`, instancia la escena `MainScene`, resuelve deep linking desde `args` o `roInputEvent`, y bombea el loop de mensajes (`wait(0, port)`). Todo lo demás vive dentro de la escena SceneGraph.
- **Nodo global (`m.global`):** [source/helpers/Global.brs](source/helpers/Global.brs) → `SetGlobalNode()` (llamado una sola vez desde `MainScene.Init()`) carga `AppConfig.json` y `AppTheme.json` del paquete, arma `apiEndPoints`, crea el `FontManager`, y guarda todo en `m.global`. Cualquier componente accede a esto vía `m.global.appConfig` / `m.global.appTheme` / `m.global.Fonts`, sin pasar props manualmente.
- **`MainScene` como router manual:** [components/scene/MainScene.brs](components/scene/MainScene.brs) es el controlador central — no hay un sistema de rutas declarativo. Por cada página existe un par `ShowXPage()` / `GetXPageObject()`: el segundo crea (o reutiliza) el nodo SceneGraph de esa página como hijo de `gPageContainer`, y el primero lo empuja al stack de navegación. Sigue este patrón al agregar una página nueva.
- **`ViewStackManager`:** [source/managers/ViewStackManager.brs](source/managers/ViewStackManager.brs) es una pila simple de nodos (`ShowScreen`/`HideTop`/`ReplaceScreen`/`FocusTop`) que `MainScene` usa para mostrar/ocultar páginas y devolver el foco. El botón `back` se maneja centralizado en `MainScene.HandleBackKey()` (dentro de `OnkeyEvent`), no en cada página.
- **Llamadas a red vía Task nodes (patrón async):** las páginas nunca golpean la red directo. Crean un nodo `roSGNode` de tipo Task (`components/tasks/ContentAPIAction`, `AuthAPIAction`, `DAIPlayerTask`), setean `functionName` + `params`, observan el campo `result`, y disparan con `control = "RUN"`. El `.brs` del Task (ej. [components/tasks/ContentAPIAction/ContentAPIAction.brs](components/tasks/ContentAPIAction/ContentAPIAction.brs)) despacha por nombre de función hacia la capa de API y escribe la respuesta en `m.top.result` — corre en su propio hilo, evitando bloquear la UI. Ver `MainScene.GetConfig()` / `OnGetConfigAPIResponse` como ejemplo completo del ciclo.
- **Capa de API en dos niveles:**
  - [source/apis/BaseRequests.brs](source/apis/BaseRequests.brs): HTTP crudo sobre `roUrlTransfer` (`getRequest`/`postRequest`/`deleteRequest`), siempre retorna `{isSuccess, response}` o `{isSuccess:false, code, reason}` vía los helpers `success()`/`fail()`.
  - [source/apis/ContentAPI.brs](source/apis/ContentAPI.brs): compone esas llamadas en endpoints concretos del backend. **Hoy todavía habla el patrón MiCHV/rudo.video** — es el archivo que hay que reescribir cuando se aborde el Paso 3+ (ver "Contexto del backend de 13go" arriba).
- **Persistencia local:** [source/managers/RegistryManager.brs](source/managers/RegistryManager.brs) envuelve `roRegistrySection` (sección `Canal13GoAuth`) y guarda entre lanzamientos la sesión del gateway (`authData`: `userId`/`accessToken`/`refreshToken`/`expiresIn`) y el perfil elegido (`currentProfile`) — el equivalente del `localStorage` `@auth_session`/`currentProfile` de `c13_reloaded`.
- **Deep linking:** el formato de `contentId` es `clave=valor` separado por `|` (ej. `programId=X|segmentId=Y|seasonId=Z|episodeId=W`), parseado en `MainScene.splitDeeplinkingData()`. `mediaType` soportados: `movie`, `season`, `episode`, `series`, `live` (`IsSupportedDeepLinkMediaType`). Los VOD/movie deeplinks resuelven vía `programs/get` y la app abre el primer episodio reproducible.

## Estructura del proyecto (`roku_13go`)

Mapa de carpetas/archivos reales (no hay build system — cada componente SceneGraph es un par `Nombre.xml` + `Nombre.brs` en la misma carpeta). Actualizar esta sección cuando se agreguen/eliminen componentes o archivos de primer nivel.

```
roku_13go/
├── manifest                            # metadata del canal (title, build_version, splash/icons)
├── bsfmt.json                          # config del formateador brighterscript-formatter
├── source/
│   ├── main.brs                        # entry point: roSGScreen + MainScene, deep linking, message loop
│   ├── apis/
│   │   ├── BaseRequests.brs             # HTTP crudo (roUrlTransfer): getRequest/postRequest/deleteRequest
│   │   └── ContentAPI.brs               # endpoints concretos (feed 13.cl, gateway Firebase) sobre BaseRequests
│   ├── managers/
│   │   ├── ViewStackManager.brs          # pila de navegación (ShowScreen/HideTop/ReplaceScreen/FocusTop)
│   │   ├── RegistryManager.brs           # roRegistrySection: sesión del gateway + perfil elegido
│   │   └── FontManager.brs               # carga DMSans-* y expone m.global.Fonts
│   ├── helpers/
│   │   ├── Global.brs                    # SetGlobalNode(): arma m.global (appConfig/appTheme/Fonts/apiEndPoints)
│   │   ├── FocusHandler.brs              # helpers de foco compartidos (SetFocus, etc.)
│   │   ├── HelpFuncs.brs                 # isValid/isNonEmptyString/getValueFromProps y otros utilitarios
│   │   ├── ImageUtils.brs                # GetImageURL() — resuelve URLs de imagen por tipo/tamaño
│   │   ├── PlayerUtils.brs               # sesión DPS del reproductor (dpssid/ndvc/sid/platform) — hlsSessionService.ts
│   │   └── RokuUtils.brs                 # utilitarios varios de plataforma
│   └── data/
│       ├── AppConfig.json                # feed 13.cl + gateway Firebase + CDN canal-13
│       ├── AppTheme.json                 # paleta de colores real de 13go
│       ├── LocalMenu.json                # datos del menú lateral (TopMenu)
│       └── PlayerButtonsItems.json       # config de botones del reproductor
├── components/
│   ├── scene/MainScene.{brs,xml}         # router manual: ShowXPage()/GetXPageObject() por página, HandleBackKey, deep linking
│   ├── pages/                            # una carpeta por pantalla (ver mapa 1:1 con c13_reloaded/src/pages más abajo)
│   │   ├── SplashScreen/                  # splash en video (SplashView): splash_tv del feed, antes de la primera pantalla
│   │   ├── OnboardingPage/                # bienvenida (LoginView de c13_reloaded): marca + botón "Iniciar sesión"
│   │   ├── DeviceLinkPage/                # vincular TV: QR + código, polling contra el gateway (ConnectView)
│   │   ├── EditorProfilesPage/            # "¿Quién anda ahí?": selección de perfil + cerrar sesión (WhosThere)
│   │   ├── HomePage/
│   │   │   ├── HomePage.{brs,xml}         # Home config-driven: arma filas según configuracion-portada
│   │   │   └── HomePageParser.brs         # parsers de shape de datos del feed → nodos de UI (ProgramItemNode)
│   │   ├── ProgramsPage/                  # catálogo VOD (feed/programas, agrupado por categoría)
│   │   ├── CategoryDetailPage/            # detalle de una categoría
│   │   ├── ProgramPage/                   # vista de programa (ProgramView): cabecera + categorías + grilla de capítulos
│   │   │   ├── ProgramPage.{brs,xml}       # orquesta: datos, favoritos, reanudar, selector de categoría, foco
│   │   │   └── ChapterCardItem.{brs,xml}   # capítulo de la grilla (MarkupGrid): imagen 16:9, duración, candado, título
│   │   ├── PlayerPage/                    # reproductor de capítulos (PlayerView)
│   │   │   ├── PlayerPage.{brs,xml}        # cadena de datos, sesión DPS, Video, tracking, calidades
│   │   │   ├── PlayerControls.{brs,xml}    # barra superior, seekbar, botones y lista de calidades, panel de episodios y "A continuación" (foco virtual)
│   │   │   └── PrerollAdsTask.{brs,xml}    # anuncio VAST antes del capítulo con RAF (Roku_Ads)
│   │   ├── DetailPage/                    # detalle heredado de MiCHV — ya solo lo usa el deep link (pendiente migrar)
│   │   ├── LivePage/                      # En vivo: video + overlay de canales (zapping)
│   │   │   ├── LivePage.{brs,xml}          # video, overlay (cabecera+grilla), foco, timer 10s
│   │   │   ├── LiveChannelRow.{brs,xml}    # fila de canal: logo circular + programas
│   │   │   └── LiveProgramItem.{brs,xml}   # caja de programa (título + hora o punto EN VIVO)
│   │   ├── RadioPage/                     # radios: player (Audio/HLS) + grilla de tarjetas cuadradas
│   │   ├── EventDetailPage/               # detalle de evento en vivo
│   │   ├── SearchPage/                    # búsqueda (teclado virtual + grilla de resultados)
│   │   │   ├── SearchPage.{brs,xml}        # orquesta: teclado, filtrado local, foco, grilla
│   │   │   ├── SearchKey.{brs,xml}         # una tecla del teclado virtual
│   │   │   ├── SearchResultItem.{brs,xml}  # card vertical de resultado (imagen + borde de foco)
│   │   │   └── SearchResultsGrid.{brs,xml} # MarkupGrid custom: "izquierda" en col 0 vuelve al teclado
│   │   ├── MyListPage/                    # favoritos / mi lista — pendiente 4d.5
│   │   ├── AccountPage/                   # "Mi Cuenta" (AccountView): datos, perfiles + editar, cerrar sesión
│   │   │   ├── AccountPage.{brs,xml}       # orquesta: userInfo/perfiles/avatares, foco virtual, logout
│   │   │   └── AccountProfileItem.{brs,xml} # avatar circular + nombre + botón redondo "editar"
│   │   ├── EditProfilePage/               # "Editar perfil" (EditProfileView): nombre + teclado + grilla de avatares
│   │   │   ├── EditProfilePage.{brs,xml}   # orquesta: datos del perfil/avatares, teclado (reusa SearchKey), foco, guardar
│   │   │   └── EditAvatarItem.{brs,xml}    # avatar de la grilla (8.5vw, borde naranjo con foco)
│   │   └── VideoPlayer/                   # PlayerOverlay (UI) + PlayerTask (Task de reproducción)
│   ├── controls/                          # componentes reutilizables dentro de las páginas
│   │   ├── focusableGroup/FocusableGroup.{brs,xml}  # scroll vertical + foco entre filas del Home
│   │   ├── SliderView/                    # fila horizontal genérica (RowList), usada en Home/ProgramsPage
│   │   ├── HeroSlider/                    # hero "Destacados" del Home (variant compact/full) — banner simple arriba
│   │   ├── MonumentalCard/                # carrusel "categoria_destacada" del Home (banner grande, recorte anclado a la imagen)
│   │   ├── TopMenu/                       # sidebar vertical expandible (íconos + perfil) — TopMenu/MenuContent/MenuContentItem
│   │   ├── TabView/                       # tabs (ej. dentro de DetailPage)
│   │   ├── ItemComponents/                # tarjetas de ítem, una carpeta por tipo de fila/grilla
│   │   │   ├── CommonItemComponent/        # tarjeta genérica: portrait/landscape/ranking/circle (señales-radios)
│   │   │   ├── RadioCardItem/              # tarjeta cuadrada de radio (RadioPage): fondo color + borde + logo
│   │   │   ├── TrackingCardItem/           # tarjeta de "Seguir viendo" (imagen 16:9, barra de avance, programa + capítulo)
│   │   │   ├── ProfileListItem/            # avatar circular de "¿Quién anda ahí?"
│   │   │   └── EPGGridItem/ , RowTitleItem/ , PlayerControlComponent/
│   │   ├── ItemNode/                      # ProgramItemNode.xml / EpisodeItemNode.xml — nodos de datos (setFields)
│   │   ├── Dialogs/                       # ExitConfirmation, RecommendDialog
│   │   └── CustomButton/ , PageLoader/ , ToastMessage/ , NextEpisodeComponent/ , ShortDetailsView/
│   └── tasks/                             # llamadas a red vía Task nodes (patrón async, corren en su propio hilo)
│       ├── ContentAPIAction/               # despacha hacia ContentAPI.brs
│       ├── AuthAPIAction/                  # despacha hacia el gateway (deviceCode/deviceToken/refreshToken/firebase)
│       └── DAIPlayerTask/                  # DAI de Google (SDK IMA) en las señales en vivo libres (convertCompanionToRAF.brs)
├── images/                                # assets del canal (íconos, focos, masks, overlays) — ver "Assets de marca reales" arriba — `images/player/`: íconos y controles del reproductor
├── fonts/                                 # DMSans-Regular/Medium/Bold.ttf
└── rasp/                                  # scripts RASP (QA/certificación) — aún referencian MiCHV, pendiente actualizar
```

**Regla práctica:** si vas a tocar algo del Home, primero mira `HomePage.brs` (orquestación) + `HomePageParser.brs` (shape de datos) + el control específico de la fila (`SliderView`, `HeroSlider` o `MonumentalCard`) — casi todo bug de layout/foco del Home pasa por la combinación de esos 2-3 archivos.

## Estado actual (ir marcando)

- [x] Paso 1 — Copia de `roku-chv` a `roku_13go`, `.git` reiniciado.
- [x] Paso 2 — `manifest`: `title=13GO`, `build_version=090320260`.
- [x] Paso 3 — `AppConfig.json` (feed 13.cl + gateway Firebase + CDN `canal-13`) y `Global.brs::GetApiEndPoints()` reescritos. Nota clave descubierta: auth y perfiles en 13go NO son endpoints REST por acción — es un único `gatewayUrl` (`https://rudo.video/gateway/13go/`) diferenciado por el campo `action`/`path` del POST (ver `c13_reloaded/src/features/auth/services/authentication.ts` y `src/services/profileService.ts`). Eso se resuelve recién en el Paso 4, al reescribir `ContentAPI.brs`/`BaseRequests.brs`.
- [x] Paso 4a — tema/colores: `AppTheme.json` con paleta real de 13go (`#FA6428` naranjo, fondos `#08090C`/`#1C1D28`, acento `#ffcf04`), mapeada revisando el uso real de cada clave en `components/`.
- [x] Paso 4b — fuentes: `FontManager.brs` y las ~35 páginas/controles que lo consumían migrados de Poppins a DMSans (`fonts/DMSans-*.ttf`), incluyendo renombrar `poppins*` → `dmSans*` en todo el código y corregir 3 rutas de fuente hardcodeadas fuera de `FontManager.brs` (`LoginPage.brs`, `SignUpPage.brs`, `OnboardingPage.brs`).
- [x] Paso 4c — íconos/splash/logo placeholder con marca 13go real (logo `c13_reloaded/src/assets/images/logos/13-go-logo.png` compuesto sobre fondo `#08090C`, tamaños verificados contra la documentación oficial de Roku: íconos 540×405/290×218/246×140, splash 1920×1080/1280×720/720×480). Es un placeholder centrado, no un diseño "a sangre" — pendiente de refinar cuando haya assets de diseño definitivos.
- [ ] Paso 4d — reescritura de `ContentAPI.brs`/`BaseRequests.brs`: auth y perfiles van por el gateway único (`action`/`path`, ver Paso 3), catálogo por el feed de 13.cl. `BaseRequests.brs` hoy no soporta ese patrón, hay que revisarlo función por función contra `c13_reloaded/src/services/*.ts` y `src/features/auth/services/*.ts`. Hallazgo clave: el Home de 13go es **config-driven** (`configuracion-portada` trae una lista de secciones con distinto `despliegue`, varias apuntan a URLs que vienen dentro de la propia respuesta de `configuracion`), no una lista fija de categorías como MiCHV — esto también va a tocar `MainScene.brs`/`HomePage.brs`, no solo la capa de API. Mini-roadmap acordado:
  - [x] 4d.1 — `GetConfig` end-to-end: `ContentAPI.brs::ContentAPI__GetConfig()` (GET público al feed, sin `client`) + `MainScene.OnGetConfigAPIResponse()` con campos reales (`logo_blanco`, `fondo_bienvenida`; `vastURL`/`urlTVVincular` no existen en este config, quedan en `""` hasta los sub-pasos de ads/device-link). De paso, renombradas las ~30 funciones internas de `ContentAPI.brs` de `TVCHVContentAPI__*` a `ContentAPI__*` (prefijo genérico, sin nombre de cliente).
  - [x] 4d.2 — Catálogo VOD completo (`ContentAPI__GetPrograms` → `feed/programas`, agrupado por categoría, sin paginar) conectado a `ProgramsPage.brs`. Aclarado con el propio `c13_reloaded` (`hooks/use-vod-data.ts`) que este feed alimenta la pantalla VOD/catálogo, no el Home — `HomePage.brs::GetAllCategories` sigue roto/MiCHV hasta el sub-paso 4d.3. Nota de datos: en cada item del feed, `key` es el id de la **categoría** (no del programa) — se usa `id` como key real del programa. Queda pendiente `GetProgramDetails`/`DetailPage.brs` (detalle de programa, otro sub-paso).
  - [x] 4d.3 — Home config-driven (armado de filas según `configuracion-portada`). Sub-dividido:
    - [x] 4d.3-A — esqueleto de orquestación (`ContentAPI__GetHomeConfig` trae `configuracion-portada`, `MainScene` guarda el config completo en `m.global.homeConfig` porque varias URLs de sección son dinámicas) + sección `destacados` completa end-to-end (hero + fila), verificado en vivo contra el feed real (shape de episodio: `title`/`image`/`bajada`/`nid`, no el shape de programa del catálogo).
    - [x] 4d.3-B — resto de tipos de sección conectados, todos verificados en vivo: `categoria_carrusel`/`categoria_destacada` (`feed/categorias/{id}` → `{programs:[...]}`), `top10` (`masvistos` → `{data:[...]}`), `senales` (`senales` → array de canales en vivo), `radios` (`radios` → array). Cambio de arquitectura: `ProcessNextHomeSection()` recorre las secciones de a una en secuencia (no en paralelo) para respetar el orden del CMS. Se recicló el endpoint/función muerta `GetAllCategories` como `GetCategoryPrograms`, y se renombró `ContentAPI__GetFeaturedSliderPrograms` → `ContentAPI__GetJsonByUrl` (genérico, ahora se reutiliza para destacados/top10/señales/radios). Nota: señales y radios se ven en el Home pero todavía no reproducen nada — eso requiere reproductor de audio/canal en vivo, pendiente.
    - **Verificado end-to-end con `brs-node` (ver "Comandos"):** el canal corre de punta a punta contra los feeds reales de 13go sin crashear, procesando las 8 secciones activas en el orden correcto. Esa prueba encontró un bug real (no solo de transcripción): `destacados_principales` y `masvistos` vienen envueltos como `{"data": [...]}` en el JSON crudo, que sumado al wrapper propio de `handleApiResponse()` da dos niveles (`apiResponse.data.data`, no `apiResponse.data`) — `OnGetFeaturedSliderProgramsAPIResponse` y `OnGetHomeTop10APIResponse` en `HomePage.brs` usaban solo un nivel. `senales`/`radios` sí son arrays planos (un solo nivel está bien ahí).
  - [x] 4d.4 — Auth + perfiles vía gateway (`action`/`path`). Hallazgo central: **13go no tiene login con correo/contraseña** — el único camino en TV es el *device linking* (`LoginView` → `ConnectView` en la web), así que se borraron `LoginPage`/`SignUpPage` (heredadas de MiCHV) en vez de portarlas. Detalle de lo implementado:
    - **Capa de API** (`ContentAPI.brs`): bloque nuevo sobre un único `ContentAPI__GatewayPost()` (POST `x-www-form-urlencoded` — verificado con `curl` que el gateway acepta form-urlencoded, no hace falta `multipart`, así que `postRequest()` sirve tal cual). Acciones: `deviceCode`, `deviceToken`, `refreshToken`, `authContent`, y `firebase` (con `path`) para `/userProfile/{uid}`, `/userInfo/{uid}`, `/userAccount/{uid}/profiles` y `/appData/avatars`. El token va SIEMPRE como campo del form, no en el header `Authorization`.
    - **Sesión** (`MainScene.RestoreSession()`, calcado de `checkUserSession()` del `AuthProvider`): lee `authData` del registry → valida el `exp` del JWT → si venció pide `refreshToken` → recién ahí `/userProfile` + `/userInfo` → `isUserLoggedIn`. Si el refresh también falla, se borra la sesión y el canal sigue como invitado. Helpers nuevos en `HelpFuncs.brs`, verificados con tests unitarios en `brs-cli`: `DecodeJwtPayload()` (base64url → `roByteArray.FromBase64String`), `IsJwtValid()`, `IsoUtcToEpoch()` (parseo manual, porque `roDateTime.FromISO8601String` ya nos falló en `LivePage`) y `FirestoreString()`/`FirestoreArray()` (Firestore envuelve cada campo por tipo: `{"name":{"stringValue":"..."}}`).
    - **Pantallas**: `OnboardingPage` = bienvenida (logo + "Regístrate y descubre 13Go" + botón + QR a 13go.cl); `DeviceLinkPage` = QR con el código adentro (`13go.cl/tv?code=...`) + código grande, polling cada `interval` segundos y renovación automática del código al expirar; `EditorProfilesPage` = "¿Quién anda ahí?" con los avatares reales (`<url de /appData/avatars><avatar>.jpg`) y un botón "Cerrar sesión".
    - **Decisión de flujo:** el canal NO fuerza login al arrancar (a diferencia del `AuthGuard` de la web) — sigue entrando como invitado a la Portada, y el login se abre desde el ítem "Mi cuenta" del sidebar. **Reemplazada (2026-09-24, pedido del usuario):** sin usuario el canal va al login igual que la web (ver la entrada del splash en el Paso 5).
    - [x] Bug real reportado por el usuario: tras vincular el dispositivo por primera vez, "¿Quién anda ahí?" dejó de aparecer — iba directo a la Portada, y solo se podía llegar al selector entrando de nuevo a "Mi cuenta". Causa: `MainScene.OnGetUserInfoAPIResponse()` (que corre tanto después de vincular como al relanzar la app con una sesión guardada) siempre saltaba el picker si ya había un `currentProfile` guardado en el registry — pero eso NO es lo que hace `c13_reloaded`: `ConnectView.tsx` navega a `/whosthere` **siempre** al autenticarse, sin mirar si ya había un perfil elegido antes; el salto directo solo aplica al relanzar la app con sesión ya existente. Fix: nuevo flag `m.skipProfilePickerIfSaved` en `MainScene` — `true` solo en `RestoreSession()`/`OnRefreshTokenAPIResponse()` (relanzamiento), `false` en `OnDeviceLinked()` (vinculación recién completada) — y `OnGetUserInfoAPIResponse()` ahora chequea el flag además de si hay perfil guardado.
    - [x] Bug real reportado por el usuario ("problemas con el foco en Quién anda ahí"), encontrado con `brs-node` + ECP (simulando teclas de verdad): el foco no se quedaba en `EditorProfilesPage` en absoluto — se lo robaba el Home, que sigue cargando sus ~8 secciones en segundo plano después de `StartApp()` aunque el usuario ya haya navegado a otra pantalla encima. Dos focos de robo encontrados: `HomePage.brs::manageFocus()` (llamado cada vez que termina de cargar una fila) y `MonumentalCard.brs::updateMeta()` (el carrusel "categoria_destacada", que llama `SetFocus(m.bWatchNow)` cada vez que actualiza su item actual) — ninguno de los dos chequeaba si su pantalla/nodo seguía siendo la que tenía el foco antes de robárselo de vuelta. Nota de paso: `HeroSlider.brs` tiene una rama `variant="monumental"` con el mismo bug, pero es código muerto — nada la usa, el carrusel real vive en el componente separado `MonumentalCard` (se corrigió igual por prolijidad, sin efecto real). Fix: `manageFocus()` sale temprano si `not m.top.visible`; `MonumentalCard.brs` solo llama `SetFocus(m.bWatchNow)` si `m.top.isInFocusChain()` (o sea, si el foco ya estaba en este carrusel, no si está en otra pantalla). Con eso resuelto, se reescribió `EditorProfilesPage.brs::onKeyEvent()` con navegación explícita y acotada, pedida por el usuario: desde "Cerrar sesión" solo "abajo" hace algo (arriba/izquierda/derecha se bloquean, no burbujean al sidebar); en los perfiles solo "arriba" (va al botón) hace algo además de izquierda/derecha entre perfiles — que ya maneja nativamente el `MarkupGrid`, este código solo se ejecuta cuando el grid ya no puede moverse más (primer/último perfil) y bloquea ahí en vez de dejar que el evento siga subiendo y salte al sidebar. Verificado con ECP: las 4 flechas quedan contenidas dentro de la pantalla como se pidió.
    - Borrado de paso: `AddEditProfilePopup`, `AvatarList`, `AvatarListItem`, `TopProfilePopupItem` y `ProfileAvatarUtils.brs` (todos hablaban el API REST de perfiles de MiCHV, que en 13go no existe). Crear/editar/borrar perfiles queda pendiente (en la web es otra pantalla, `EditProfileView`); la función `ContentAPI__UpdateProfile` ya está escrita pero sin UI que la use.
  - [ ] 4d.5 — Favoritos / historial / EPG.
    - [x] Bug real (crash) encontrado con telnet apenas el login funcionó por primera vez: `HomePage.brs::fetchAndStoreWatchHistory()` corre siempre que `m.scene.isUserLoggedIn = true` (para armar la fila "Seguir viendo", heredado de MiCHV) y llama `ContentAPI__GetAllWatchHistory()`, que lee `apiEndPoints.GetAllWatchHistory` — una clave que nunca se agregó a `Global.brs::GetApiEndPoints()` porque 4d.5 sigue pendiente. `path` quedaba `Invalid` y `postRequest(path as string, ...)` tiraba `Type Mismatch: Unable to cast "Invalid" to "String"` — con telnet conectado esto suspende todos los threads (se ve como la app "pegada" en el spinner "Please wait", no como un crash/cierre). Mismo problema latente en `ContentAPI__GetWatchHistory`/`ContentAPI__AddWatchHistory` (usadas por `DetailPage.brs`/`VideoPlayer.brs`, resume/analytics). Fix: las tres funciones ahora chequean `isNonEmptyString(path)` antes de llamar `postRequest` y devuelven `error(...)` si el endpoint no existe — los callers ya manejaban bien una respuesta de error (chequean `isValid(response.data)` en cascada), así que no hubo que tocarlos. Sin este guard, cualquier pantalla que dispare estas tres llamadas con sesión iniciada crashea igual — hay que recordar sacar el guard cuando se implemente 4d.5 de verdad.
- [ ] Paso 5 — Paridad visual con `c13_reloaded` (sidebar + Home). Decisión (2026-09-07): calcar lo más posible el diseño real de la web, verificado en vivo (login real, `npm run dev` de `c13_reloaded` en `localhost:3000`) — no solo el código. Techo realista: SceneGraph no tiene CSS ni las animaciones de un navegador, se apunta a "lo más parecido posible" (colores/proporciones/iconografía), no pixel-perfect. Hallazgo clave: cada tipo de sección del Home tiene un estilo visual propio y distinto en la web (`categoria_carrusel` = tarjetas con badge "Premium", `categoria_destacada` = carrusel "monumental" tipo banner, `top10` = tarjetas con número de ranking gigante, `senales`/`radios` = círculos con logo) — hoy en Roku todas se ven igual (fila genérica `SliderView`+`ProgramItemNode`). Mini-roadmap:
  - [x] Sidebar 1/3 — riel vertical de íconos (sin expandir/colapsar todavía). `TopMenu`/`MenuContentItem` pasaron de grilla horizontal de texto (7 cols x 1 fila) a vertical de íconos (1 col x 5 filas), con los 5 ítems calcados de `c13_reloaded` (Portada/Programas/En vivo/Radios/Búsqueda — se sacó "Mi Lista" del menú principal, decisión explícita del usuario). Íconos reales exportados de `c13_reloaded/src/assets/images/iconos/*.svg` (SVG, Roku no los renderiza) a PNG transparente vía `sharp` en `images/icons/sidebar/`. Color del ícono por estado (blanco foco / naranjo activo / gris inactivo) vía `blendColor`, igual que el texto lo hacía en la versión vieja. Logo y avatar/login quedan sin reubicar (es la parte 3/3) — layout transitorio esperado.
  - [x] Sidebar 2/3 — expandir/colapsar con foco: `MenuContent` ganó el campo `isExpanded` (mismo patrón que `isSelected`), `TopMenu.OnFocusChild()` lo propaga a todos los items cuando `topMenuGrid` tiene foco (`SetMenuExpanded()`), y cada `MenuContentItem` muestra/oculta su `Label` de título con el mismo color que el ícono (blanco foco / naranjo activo / gris inactivo). Hubo que agrandar `itemSize` del grid (90→260 de ancho) porque `MarkupGrid` recorta cada item a su tamaño declarado.
  - [x] Sidebar — degradado al expandir: en `c13_reloaded`, `.menu.active::before { background: linear-gradient(90deg, var(--color-3), transparent); width: 90vw; }` — un degradado de fondo que aparece detrás del contenido cuando el sidebar se expande, para separarlo visualmente. Se agregó `pExpandGradient` a `TopMenu.xml` reutilizando el mismo asset `hero_fade_left.png` del hero (mismo color/dirección), togglada en `SetMenuExpanded()`.
  - [x] Sidebar 3/3 (avatar/login) — resuelto integrándolo **como primer ítem de la grilla** (la opción "más fiel a `c13_reloaded` pero más riesgo" de las dos que quedaban pendientes): `LocalMenu.json` ahora arranca con "Mi cuenta", `MenuContent` ganó `isAvatar`/`avatarUri`/`isPremium` y `MenuContentItem` dibuja un avatar circular (`MaskGroup` + `profile_mask.png`) en vez del ícono plano, con anillo amarillo `#ffcf04` si la suscripción está activa (`status` = `success`/`light`, igual que el `.premium` del CSS). Seleccionarlo abre el login (sin sesión) o "¿Quién anda ahí?" (con sesión). Se eliminó todo el bloque viejo de MiCHV que vivía arriba a la derecha (`gMyProfile`, `gProfilePopup`, popup de agregar/editar perfil), que no existe en la web. **Ojo con el riesgo conocido:** `SetupContent()` ya no asigna `m.topMenuGrid.itemSelected = 0` (eso disparaba el observer al arrancar — era la causa real del "bug #4" que antes se atribuía al foco); ahora marca "Portada" como activa vía `jumpToItem` + `isSelected`, porque con "Mi cuenta" en el índice 0 esa asignación habría abierto el login sola al iniciar el canal.
  - [x] Bug real reportado por el usuario: "el ícono del perfil no se ve correctamente, además falta la placa de premium". Dos causas: 1) el avatar (`mgAvatar`/`pAvatar`, 52×52) medía distinto que el resto de los íconos del riel (`itemIcon`, 40×40) — la web usa el mismo `max-width:3.2vw` para TODOS los íconos del `.menu ul li img`, incluido el avatar, así que el tamaño distinto se veía inconsistente. 2) la placa "Premium" nunca se implementó — solo se hizo el anillo (`.menu .avatar .premium`), pero el CSS real tiene ADEMÁS un `.premium::after` — una pastilla amarilla con el texto "Premium" en negro, asomando debajo del anillo (`bottom:-19%`). Fix: avatar bajado a 40×40 (mismo tamaño/posición que `itemIcon`), y placa nueva (`pPremiumBadgeBg` + `lPremiumBadge`) con texto negro `dmSansBold18` (el Bold más chico importado, la web usa 0.6vw ≈ 11px, ilegible en TV) sobre un rectángulo generado con `sharp`.
  - [x] Ajuste tras feedback del usuario probando en un Roku real: el anillo "se ve entrecortado" y el texto "Premium" se corta ("Pre..."). Causa 1: reusé `circle_ring_thin.png` (pensado para los círculos de señales/radios de 192px, trazo fino) escalado a 46px — a ese tamaño el trazo queda casi de 1px y se ve con gaps/aliasing en una TV real. Causa 2: la placa (`pPremiumBadgeBg`/`lPremiumBadge`) medía 46px de ancho (el mismo que el anillo, calcando el CSS real) pero `dmSansBold18` para el texto "Premium" necesita ~75-80px — se cortaba. Fix: nuevo anillo dedicado `images/masks/avatar_premium_ring.png` (generado a 184px con trazo proporcionalmente más grueso y reescalado a 46px, en vez de reusar un asset pensado para otro tamaño — verificado limpio con `sharp` antes de aplicarlo) y placa agrandada a 80×20 (ya no calca el ancho del anillo, prioriza que el texto entre completo).
  - [x] Tercera ronda de feedback del usuario probando en el Roku real: "al sidebar le falta un poco más de padding", "la P de Premium no se alcanza a ver, queda oculta en el borde izquierdo", y "el fondo del logo del perfil no es circular, parece un cuarto de circunferencia". Tres causas: 1) `topMenuGrid` (el riel) arrancaba en `x=25` — muy pegado al borde real de una TV con overscan (mismo fenómeno ya documentado en este proyecto para el borde DERECHO de las tarjetas del Home, "colchón de seguridad" de 46px). 2) la placa "Premium" agrandada a 80px en la ronda anterior quedaba centrada bajo el avatar (`translation.x=5`), extendiéndose 20px más a la izquierda que el propio ícono (`x=25`) — con el riel ya pegado al borde, esos 20px extra caían justo en la zona que el overscan corta, comiéndose la "P". 3) `pFocusBg` (el fondo de foco genérico, un rectángulo con esquinas redondeadas de 76×76 colapsado) se usaba también para el ítem del avatar — sus esquinas, mucho MENOS curvas que un círculo, sobresalían detrás del avatar circular (40×40) más chico, viéndose como una cuña/cuarto de círculo en cada esquina. Fix: `topMenuGrid`/`pLogoImage` corridos +20px (`x=45`/`x=120`); placa/anillo "Premium" alineados al borde izquierdo del ícono (`x=25→x=20`, ya no centrados, priorizando no acercarse más al borde) en vez de extenderse a la izquierda; y nuevo `pAvatarFocusBg` — un círculo blanco dedicado (`images/masks/avatar_focus_bg.png`, generado con `sharp`, 50×50) que reemplaza a `pFocusBg` específicamente para el ítem del avatar (`MenuContentItem.brs::UpdateIconColor()` ahora bifurca por `m.isAvatarItem`).
  - [x] El fix anterior de `pAvatarFocusBg` **no alcanzó** — el usuario reportó que el fondo circular del avatar seguía viéndose como un cuarto de circunferencia. Investigando más profundo con `sharp` (`.metadata()`), se encontró que **tanto `profile_mask.png` (el mask circular del `MaskGroup`) como `default_user.png` (el avatar de reemplazo) son PNG con paleta indexada** (`isPalette: true`), no RGBA verdadero — un formato con transparencia vía índice de paleta en vez de canal alfa por-píxel completo, con antecedentes de compatibilidad inconsistente en el decodificador nativo de Roku para `MaskGroup` (`sharp`/libvips los decodifica bien, pero eso no garantiza que el firmware del Roku real lo haga igual — mismo tipo de brecha simulador-vs-hardware ya visto antes en este proyecto). Sospecha reforzada por la exposición diferencial: el mismo asset a maskSize `[188,188]` en `ProfileListItem` (¿Quién anda ahí?, casi sin downscale del original ~189px) nunca fue reportado con este problema, pero a `[40,40]` en el sidebar (downscale ~4.7x) sí — un bug de decodificación de paleta se notaría más al reducir agresivamente. Fix: ambos assets regenerados desde cero como PNG RGBA verdadero (`sharp({palette:false})`, sin paleta) — `profile_mask.png` (círculo sólido limpio) y `default_user.png` (mismo diseño, silueta de persona). Reemplazados in-place, así también benefician a `ProfileListItem`/`CommonItemComponent`/`AccountPage`, que comparten el mismo mask. Sin poder confirmar 100% que esta era la causa real sin verlo en el Roku (la simulación con `sharp` ya decodificaba bien el asset viejo también, así que esto ataca la hipótesis más fundamentada disponible, no una certeza confirmada) — pendiente de confirmación del usuario.
  - [x] (Intento descartado) Se probó una máscara dedicada chica (`avatar_mask_sidebar.png`, 80px) suponiendo que el problema era la reducción 400→40px del `maskUri` — **no resolvió nada** y se borró. Los PNG sí estaban bien (verificado con `System.Drawing`: 32bpp ARGB, círculos correctos píxel a píxel), así que la hipótesis de paleta del punto anterior también era falsa.
  - [x] **Causa real del "cuarto de circunferencia" del avatar** (aclarada por el usuario: el ícono de la persona y el foco se veían bien; lo roto era el disco azul de fondo, que aparecía como 1/4 de círculo): el `MaskGroup` aplica mal la máscara en este Roku. Hipótesis más fundamentada: el `manifest` declara `ui_resolutions=fhd` y el Roku Express (3930X) dibuja la UI a 720p, así que SceneGraph reescala todo por 2/3 — pero la máscara del `MaskGroup` no se reescala igual que su contenido, queda ~1.5x más grande y anclada arriba-izquierda, y lo visible es un recorte con forma de cuarto de círculo. Encaja con los dos antecedentes del proyecto: el `Rectangle` de fondo de los círculos de señales (mismo síntoma exacto, "cuarto de circunferencia", resuelto sacándolo del `MaskGroup`) y `SearchResultItem` ("el `MaskGroup` de Roku mapeaba mal el mask", resuelto con un marco encima). Los logos de señales/radios siguen dentro de un `MaskGroup` y "funcionan" solo porque son PNG mayormente transparentes y más chicos que el círculo — el mal recorte no se nota. Fix: el avatar del sidebar ya no usa `MaskGroup`. `pAvatar` es un `Poster` directo; `default_user.png` ya es circular (esquinas transparentes); para fotos reales (los avatares de `/appData/avatars` son JPG cuadrados, la web los redondea con `border-radius`) se muestra encima `pAvatarFrame` (`images/masks/avatar_frame_sidebar.png`, cuadrado `#08090C` con agujero circular, el color de fondo del riel). El foco pasó de disco a anillo (`images/masks/avatar_focus_ring.png`) dibujado por encima del marco, para que las esquinas del marco no lo tapen. Limitación: sobre páginas cuyo fondo detrás del riel no sea `#08090C` (Radios con su imagen de fondo, En vivo con video), las esquinas del marco se verían como un cuadradito oscuro — solo con foto real, no con el avatar por defecto. **Pendiente de revisar con el mismo criterio:** `ProfileListItem` ("¿Quién anda ahí?") y `AccountPage` usan `MaskGroup` con los mismos JPG cuadrados — probablemente tienen el mismo recorte roto con fotos reales. Regla para el futuro: **no usar `MaskGroup` en este proyecto** para recortar contenido opaco; usar assets pre-recortados o un marco encima. Confirmado por el usuario en el Roku real.
  - [x] Detalle tras la confirmación: la silueta de `default_user.png` (el cuerpo) se salía del disco azul por abajo. Fix: el PNG se recortó al círculo (alfa anti-aliased según la distancia al centro, mismo diseño), así el avatar por defecto no desborda en ningún lugar donde se usa (sidebar, "¿Quién anda ahí?", cuenta).
  - [x] Placa "Premium" centrada bajo el avatar (pedido del usuario: se veía corrida a la derecha): `pPremiumBadgeBg`/`lPremiumBadge` de `x=20` a `x=5` (80px de ancho → centro en x=45, el mismo eje que el avatar y los íconos del riel). Con el riel ya corrido a `x=45`, el borde izquierdo de la placa queda a 50px del borde de la pantalla (la "P" se cortaba a 30px).
  - [x] Vista "Mi Cuenta" (pedido del usuario: "revisa c13_reloaded, debe cargar la vista de la cuenta"). Hallazgo: en la web el ítem "Mi cuenta" del sidebar navega a `/mi-cuenta` (`AccountView.tsx`), **no** a "¿Quién anda ahí?" — ese selector solo aparece al vincular. El Roku abría `EditorProfilesPage` desde el sidebar (y ahí el usuario reportó un spinner congelado, que no se reprodujo en `brs-node`; con este cambio ese camino ya no se usa desde el sidebar). Se reescribió la `AccountPage` heredada de MiCHV (estaba sin usar) calcando `AccountView.tsx` + `use-account-data.ts` + CSS `.main.regular.mi-cuenta`: título "Mi Cuenta"; dos columnas de info — "Datos personales:" (nombre y email **del JWT**, igual que `jwtDecode(accessToken)`) y "Datos de suscripción:" (`title`/`content` de `/userInfo`, "Sin datos" si no hay); "Tus perfiles" con un `AccountProfileItem` por perfil (avatar de 8vw atenuado al 60%, anillo naranjo con foco, nombre, y botón redondo de editar con el lápiz real de `iconos-editar.svg` convertido a PNG); botón "Cerrar Sesión" (pill sin relleno, mismo estilo que "Ver ahora"); fondo `bg-micuenta.png` abajo-derecha. Datos: `GetAvatarBaseUrl` + `GetProfilesData` + `GetUserInfo` en paralelo, spinner hasta que vuelven las tres. Navegación calcada de la web (norigin): izquierda/derecha entre avatares o entre botones de editar, abajo avatar→editar→cerrar sesión, arriba desde cerrar sesión al **último** editar (`handleUpFromLogout`), izquierda en la primera columna o en el botón sale al sidebar; OK en un avatar elige el perfil y va a la Portada (`setProfile` + `/home`). Foco virtual sobre `gProfiles` (`focusable="true"`, porque la raíz de la página no recupera el foco desde el botón hijo — mismo caso que el teclado de Búsqueda). En `MainScene`, "Mi cuenta" con sesión ahora es una sección más: reemplaza la página actual, deja el sidebar visible y marca "Mi cuenta" como activa (`TopMenu.UpdateSelectedTopMenu` permite marcarla solo cuando llama `MainScene`). Avatares sin `MaskGroup` (marco `#08090C` encima, como el sidebar). Verificado con `brs-node` + ECP con datos simulados (navegación completa, salida/vuelta del sidebar, OK → Portada). **Pendiente:** el botón editar no hace nada todavía (`EditProfileView` sin portar); "Cerrar Sesión" mantiene el diálogo de confirmación del Roku (la web cierra sesión directo). Confirmado visualmente por el usuario en el Roku.
  - [x] Bug: con foco en un perfil, parte del anillo naranjo quedaba tapada por un cuadrado negro. Causa: `pRing` se dibujaba antes que `pAvatarFrame`, y las esquinas del marco (cuadrado de 122px, llegan a ~86px del centro en las diagonales) pisaban el anillo (radio 77–82). Fix: `pRing` pasó a ser el último hijo de `AccountProfileItem.xml` (el anillo queda fuera de la foto, así que dibujarlo encima no tapa nada).
  - [x] Vista "Editar perfil" (OK en el lápiz de un perfil en "Mi Cuenta"), calcada de `EditProfileView.tsx` + `use-edit-profile-data.ts` + `KeyboardButton`/`AvatarItem`/`ProfileEditDisplay`/`SaveButton` + CSS `.main.regular.editar-perfil`. Layout del grid real a 1920px: título "Editar perfil" centrado sobre todo el grid; columna izquierda de 21vw (403px) con la vista previa del avatar (8vw), "Nombre:" y un input tipo píldora (`#292929`, borde `#666`, naranjo con foco), y debajo el teclado de 7 columnas de la web (a–z, ñ, 0–9, "Espacio" de 3 columnas y borrar de 2; reusa `SearchKey`, mismo color que el `#ffffff08` de la web sobre el fondo); a la derecha, en x=837 (la web usa `grid-column: 7` y las columnas implícitas vacías 3–6 no ocupan ancho pero sí sus gaps de 2vw), "Avatar:" y la grilla de 5 columnas de avatares de 8.5vw (60% sin foco, borde naranjo con foco, recorte con marco `#08090C` en vez de `MaskGroup`) con 37vw (710px) visibles y scroll por fila (`scrollIntoView` "nearest"); "Guardar cambios" centrado debajo de la grilla. Datos: `/appData/avatars` (una sola llamada trae la URL base **y** la lista `list.arrayValue.values`; la web la pide dos veces) + perfil puntual `/userAccount/{uid}/profiles/profile-{order}/` (nuevo `ContentAPI__GetProfileData` / `AuthAPIAction.GetProfileData`). Guardar usa `ContentAPI__UpdateProfile` (ya existía, `store_method=update`), muestra "Guardando..." y, como la web (`navigate('/mi-cuenta')` si el POST no falla), cierra la edición y "Mi Cuenta" recarga sus datos (`AccountPage.ReloadData`, conserva el foco en el lápiz). Navegación calcada de la web: foco inicial en la "a"; izquierda en la primera columna del teclado sale al sidebar; derecha en la última columna va al primer avatar; arriba en la primera fila va al nombre; abajo en la última fila (8, 9, Espacio, borrar) va a "Guardar"; en avatares, izquierda en la columna 0 vuelve al final de la fila equivalente del teclado (`KEYBOARD_RETURN_KEYS`: g, n, t, 0, 7, borrar) y abajo desde la última fila va a "Guardar"; OK en un avatar lo elige (cambia la vista previa). `MainScene.ShowEditProfilePage(profileId)` la apila encima de "Mi Cuenta" con el sidebar visible (back vuelve a la cuenta, como el `navigate(-1)` de la web); ir a otra sección del sidebar cierra la edición primero (si no, el reemplazo de página dejaría "Mi Cuenta" en la pila), pero elegir "Mi cuenta" estando en la edición no hace nada (la grilla puede disparar `itemSelected` solo con entrar al riel). Verificado con `brs-node` + ECP con datos simulados (navegación completa, escribir/borrar, elegir avatar, guardar → vuelta a la cuenta con recarga). Pendiente: probar el guardado con una sesión real (con token falso el gateway respondió `incorrect firestore method`, esperable sin `userId`); el input de nombre no abre un teclado propio (se edita con el teclado en pantalla, OK en el input no hace nada).
  - [x] El avatar/nombre editados se reflejan en el sidebar (pedido del usuario; la web **no** lo hace — su `Sidebar.tsx` lee el `currentProfile` del localStorage, que la edición no toca). Nuevo `MainScene.UpdateCurrentProfile(profile)`: si el perfil guardado es el que está en uso (`ProfileData.profileId`), actualiza `ProfileData` (el `TopMenu` ya lo observa y le pasa el `avatarUri` al ítem "Mi cuenta") y el `currentProfile` del registry (persiste al relanzar). Como el `profileId` no cambia, `OnProfileDataChanged` no recarga la página. Editar otro perfil distinto al activo no toca el sidebar. `EditProfilePage` lo llama al guardar, antes de volver a la cuenta. Verificado con `brs-node` + ECP (el `avatarUri` del sidebar pasa a la imagen elegida tras guardar).
  - [x] Vista de programa (bug reportado: "al entrar a un programa queda pegado"). Causa: toda tarjeta llamaba `MainScene.showDetailPage` → `DetailPage` heredado de MiCHV, que habla el API de MiCHV y nunca termina de cargar. Nueva `ProgramPage` calcada de `ProgramView.tsx` + `use-program-data.ts` + `ChapterCard.tsx`/`CategorySelect.tsx` + CSS `.programas-cabecera`/`.grilla-capitulos`: cabecera de 29.17vw con `fondo_imagen` a 85vw arriba a la derecha + degradados (mismos assets que `VodHeader`), título, bajada (el CMS la manda con HTML, se limpia a texto) y botones "Reanudar" (solo si hay progreso) / "Agregar a favoritos"·"Quitar de favoritos" (estrella de `iconos-favorito.svg`); debajo, el nombre de la categoría (o un select cuadrado con flecha si hay más de una, con dropdown que se abre **arriba**, fondo negro y opción enfocada naranja), "N videos" y la grilla de capítulos de 5 columnas (`MarkupGrid` + `ChapterCardItem`: imagen 16:9 con esquinas de 10px vía marco, brightness .5 sin foco, duración con sombra diagonal abajo a la derecha, candado + velo si `validateRestriction` lo bloquea, título en negrita). La página sube cuando el foco baja de la primera fila (index < 5 = tope, como la web). Datos: `feed/programa?v=/programas/{slug}` → `feed/programas/{slug}/categorias` → `feed/programas/{slug}?t={categoría}` (invertido si `on_air = "0"`); con sesión, además `/accountMyList/{uid}/categories/profile-{order}/items` (favorito si coincide nid/slug/id_program y no está `inactive`), `saveFavorites` (active/inactive) y `/accountTracking/{uid}/history` (reanudar el primer capítulo con progreso entre 10s y 95%). Cómo se llega: las tarjetas de programa ahora llevan `url` (`/programas/{slug}`, que traen todos los feeds: `categoria_carrusel`, `categoria_destacada`, top10, catálogo de Programas y Búsqueda; `ContentNode` ya tiene el campo `url` nativo) y `showDetailPage` la abre con el sidebar visible, apilada sobre la sección (back vuelve a donde estaba). Navegación calcada de la web (Reanudar↔Favoritos, abajo al select o al primer capítulo, arriba desde la primera fila de la grilla al select/botones, izquierda en la primera columna o en los botones al sidebar). Detalles técnicos: `getRequest` ganó `skipEncode` porque `EncodeUri()` recodifica `%` como `%25` (la categoría va con `EncodeUriComponent`); si se sale con back mientras carga, las respuestas pendientes se descartan y se apaga el spinner (si no quedaba prendido sobre la página de abajo); si el sidebar dispara la sección de abajo solo por entrar al riel ("Bug real #4"), no se saca al usuario del programa. **No portado a propósito:** "También te puede interesar" (la web pide `feed/categorias/{nombre de la pestaña}` y el feed siempre devuelve vacío, así que nunca se ve) y el video de fondo HLS de la cabecera (se usa la imagen: en este Roku la transparencia sobre `Video` no compone bien). **Pendiente:** elegir un capítulo (o "Reanudar") todavía no reproduce — falta portar `PlayerView`; sin sesión, un capítulo restringido abre el login, y bloqueado por plan debería ir a `/suscribe` (sin portar). Las tarjetas de "Destacados" (capítulos, en la web van directo al reproductor) ya no abren el `DetailPage` colgado: no hacen nada hasta que exista el reproductor. (Resuelto después: ver "las tarjetas de Destacados ya reproducen".) Verificado con `brs-node` + ECP (entrar desde el carrusel monumental, cargar 108 capítulos, cambiar de categoría con el dropdown, navegar la grilla con scroll, back a mitad de carga).
  - [x] Scroll de la vista de programa más suave (pedido del usuario: era brusco al bajar a la segunda fila y al volver a la primera). Antes `ScrollPageTo()` asignaba `gPage.translation` de golpe; ahora anima con un `Animation` + `Vector2DFieldInterpolator` (`outCubic`, 0.35s — el Home usa `linear` 0.25–0.3s en `FocusableGroup`; la curva de salida frena al final y se siente más suave), el equivalente al `behavior: 'smooth'` de la web. Si llega otro scroll a mitad de animación, arranca desde la posición actual (no salta); el dropdown del selector se posiciona con el destino del scroll (`m.pageTargetY`), no con la posición a mitad de camino. Verificado con `brs-node` + ECP.
  - [x] Tarjetas de capítulo sin redondeo y con el foco "desbordado" (reportado por el usuario). Dos causas: 1) el redondeo es un marco con esquinas `#08090C` (sin `MaskGroup`), pero detrás de la grilla todavía asomaba la imagen de fondo del programa con el degradado a medio camino — sobre ese fondo las esquinas no se funden y la tarjeta se ve cuadrada; 2) el anillo de foco reusaba `R8_T3_50PX_border.9.png`, con otra curvatura que la tarjeta, así que sus esquinas quedaban por fuera del anillo. Fix: el degradado inferior de la cabecera (`pFadeBottom`) ahora llega a `#08090C` sólido en y=700 (antes en 920; en la web en ese punto ya está ~82% oscuro) y `rGridBackground` deja sólido todo lo que queda debajo, así las esquinas del marco se funden con el fondo; y el foco usa un anillo propio `images/program/chapter_focus_ring.png` (305×172, 4px = 0.21vw, radio 10 igual que la tarjeta y que el `.card.focus .imagen::before` de la web). Regla general: si una tarjeta usa el marco de esquinas en vez de `MaskGroup`, el fondo detrás tiene que ser sólido `#08090C`.
  - [x] Ícono desalineado en el botón "Agregar a favoritos" (reportado por el usuario) — en realidad un bug de `CustomButton` que afectaba a **todos** los botones con ícono (Reanudar, Favoritos, Ver ahora, Cerrar Sesión, Guardar cambios). Medido con `brs-node`: el ícono quedaba en y=30 en vez de y=20 (10px más abajo que el centro del texto). Causa: `OnButtonHeightChange` calculaba la posición del ícono cuando todavía medía 0 (`(60 - 0) / 2`) y `SetTranslations()` —que lo corregía— solo corría si `posterImage` ya estaba seteado, lo que dependía del orden (no garantizado) en que `update({...})` aplica los campos. Fix: `SetTranslations()` reescrito y llamado ante cualquier cambio de ícono, texto, fuente, padding, margen o medidas: ícono centrado en el alto y bloque ícono + texto (gap 10px, el de `.btn`) centrado horizontalmente en el botón, midiendo el texto con `boundingRect()` de un `Label` sin ancho (mismo truco que `SetBadge()`; si la medida da 0 —`brs-node` no mide fuentes— cae a alineado a la izquierda con el padding). El usuario aclaró que el problema visible era el **horizontal**: antes la estrella quedaba pegada a la izquierda (x=10, porque `SetTranslations()` nunca corría) y el texto centrado por su cuenta en todo el botón, así que no se veían como un bloque. Refuerzo: el texto se mide con `wrap = false` (el `Label` tiene `wrap="true"` en el XML, y con wrap y sin ancho puede medir 0 → caería al respaldo); `wrap` vuelve a `true` solo si el texto no entra. Pendiente de confirmación visual en el Roku.
  - [x] Selector de categoría rehecho (el usuario: "no se ve bien"). Como la vista de programa está detrás del `AuthGuard`, se renderizó el HTML real del selector con el CSS real de `c13_reloaded` en un servidor estático de scratchpad y se midió en el navegador integrado. Hallazgo: el select **no tiene caja** — `select.titulo-2 { border: none }` le gana a `.btn` por especificidad —, es solo el texto de 40px en negrita, pegado a la izquierda (padding 0), con la flecha blanca de 24px a la derecha en un padding de 1.9vw (36px); el ancho es el de la opción **más larga** (así funciona un `<select>`), alto de una línea (`line-height: 1em`), y "N videos" va pegado debajo. Con foco solo el texto pasa a naranjo (la flecha es un `background` y no cambia). La versión anterior dibujaba un recuadro gris de 4px con padding — no existe en la web. Cambios: fuera el borde; ancho = categoría más larga + 36px (medida con `boundingRect()`); resumen en y=604 y la grilla sube a y=672 (`.content { margin-top: 2vw }`), con el degradado/fondo sólido corridos para seguir terminando justo antes de la grilla; opciones del dropdown de 61px (`padding .8vw 1.2vw` + 1.2vw). Para comparar componentes contra la web sin sesión, este truco (HTML con las clases reales + `panels_new.css` servido local) sirve cuando la ruta está protegida.
  - [x] Reproductor de capítulos (`PlayerView.tsx` + `VideoPlayer.tsx` + `use-hls-player.ts` + `use-player-analytics.ts` + `use-ads-policy.ts` + `VastPlayer.tsx`/`deviceAdService.ts` + `hlsSessionService.ts` + `ga4Service.ts`). Análisis hecho (2026-09-23), sin código todavía. Cadena de datos: `feed/video?v={link del capítulo}` (trae `category`, `vast_app`, `live_redirect`, `show`) → `feed/programa?v=/programas/{slug}` → `feed/programas/{slug}?t={categoría}` (el reproductor invierte la lista **siempre**, la vista de programa solo con `on_air = "0"`) → match por `id` → restricción (sin sesión → login; bloqueado por plan → `/suscribe`) → `api.rudo.video/api/api2/v3/media/info/{key}` con header `X-ACCESS-TOKEN` (da el `m3u8`) → si `media.restriction = "1"`, gateway `action=authContent`, `type=vod`, `id={key}` → `?st=..&ts=..&e=..`. Sesión DPS en el m3u8 y en la URL del anuncio: `dpssid` persistente (`b`+32 hex, `ndvc=1` la primera vez), `sid` nuevo por reproducción (`s`+32 hex), `platform=13GOTVRoku`; verificado que el redirector los propaga a cada calidad del m3u8 (en Roku alcanza con la URL principal). Tracking: gateway `action=contentTracking` (`token`, `profile_id=profile-{order}`, `content_id`, `event_type` progress/end, `seconds`, `duration`, `restriction`, `title`, `show`, `image`, `path`) cada 15 s de avance + `end` a ≤2 s del final, solo con sesión, sin anuncios ni en vivo. **Decisiones del usuario:** GA4 (`G-33D1K2C2YN`, `g/collect`) en **todas** las vistas con plataforma Roku; **sin Chartbeat**; **DAI solo en la señal en vivo** (IMA DAI SDK / `DAIPlayerTask`, canales con `DPSDAIAssetKey`), **no en capítulos** — los capítulos llevan el preroll VAST (`vast_app`) como la web, vía RAF (`PlayerTask`; Roku exige RAF y el SDK IMA de Roku solo hace DAI), con la política `ads_free` del feed de configuración (ver paso 5) y los ajustes a la URL de `appendAdParamsToVastUrl` (`sz` 640x480, `iu` sin `app.`, `url`/`ref`/`description_url`, `env=vp`, `correlator`, `vpos`, `impl`, `rdid`=RIDA / `is_lat` / `idtype=rida`). Plan: 1) reproducción básica (cadena de datos, restricción, `authContent`, sesión DPS, `Video`, reanudar, back al programa); 2) tracking `contentTracking`; 3) controles (seek acelerado 15→300 s aplicado a los 600 ms, play/pausa, volver al inicio, calidad, auto-ocultar 4 s); 4) "siguiente capítulo" (a 60 s del final, cuenta regresiva) + panel "Episodios"; 5) preroll VAST con RAF; 6) GA4 global. El `VideoPlayer`/`PlayerOverlay`/`NextEpisodeComponent` de MiCHV hablan otro API y se reemplazan.
    - [x] Paso 1 — reproducción básica: nueva `PlayerPage` (`components/pages/PlayerPage/`), apilada sobre la vista de programa a pantalla completa y sin sidebar (`MainScene.ShowPlayerPage({ link, slug, initialSeconds })`). Hace la cadena de `PlayerView.tsx` completa (`ContentAPI__GetEpisodeByLink` → programa → capítulos invertidos → match por `id` → restricción → `ContentAPI__GetVodMediaInfo` con `X-ACCESS-TOKEN` → `authContent` si `media.restriction = "1"` y hay suscripción activa), le agrega la sesión DPS (`source/helpers/PlayerUtils.brs`: `GetDpsSessionParams()`/`ForceSessionParams()`; el `dpssid` vive en la sección de registry `Canal13GoDevice` para no borrarse al cerrar sesión) y reproduce con el nodo `Video` (UI nativa por ahora; "Reanudar" arranca en `content.playStart`; `live_redirect = "1"` → `content.live`). Sin sesión y capítulo restringido: cierra el reproductor (`ClosePlayerPage`) y abre el login; bloqueado por plan: mensaje "Este contenido requiere una suscripción" (SuscribeView sin portar). Errores como la web ("Programa no encontrado", "Episodio no encontrado", "No se pudo cargar el video") en texto centrado sobre negro. Back: corta el video y vuelve a la vista de programa con el sidebar (nuevo caso en `HandleBackKey`: si el tope que queda es `ProgramPage`, muestra el menú); "izquierda" ya no le da foco al sidebar si está oculto (`m.gTopMenu.visible`). `ValidateRestriction()` pasó a `HelpFuncs.brs` (la usan `ProgramPage` y `PlayerPage`). Verificado con `brs-node` + ECP: programa → capítulo → cadena completa y URL final con `dpssid`/`ndvc`/`sid`/`platform=13GOTVRoku` → back a la vista de programa; capítulo restringido sin sesión → login. Confirmado en el Roku real: el video se ve bien y "Reanudar" retoma. Falta probar la firma `authContent` con un capítulo restringido.
    - [x] Paso 2 — tracking de avance (reportado por el usuario: vio un capítulo en el Roku real y al volver no aparecía "Reanudar"; faltaba este paso). `ContentAPI__UpdateTracking` / `AuthAPIAction.UpdateTracking` (gateway `action=contentTracking`, mismos campos que `TrackingService.updateTracking`). `PlayerPage.OnVideoPositionChange()` calca `onPlaybackProgress`: un `progress` cada 15 s de avance y un `end` a ≤2 s del final (sin tracking en vivo ni sin sesión/perfil). Los envíos van por `MainScene.RunBackgroundTask()` (la escena guarda la referencia hasta que la task termina), así el último reporte no se pierde al apretar back — la web tampoco lo cancela. `ProgramPage` vuelve a pedir el historial al hacerse visible de nuevo (`LoadTracking()` desde `OnVisibleChange`; en la web `ProgramView` se vuelve a montar) y `OnTrackingResponse` calcula el capítulo a reanudar antes de tocar el botón (sin parpadeo; si ya no hay avance, lo oculta). Igual que la web: el avance se guarda cada 15 s, así que "Reanudar" arranca hasta 15 s antes de donde se dejó, y si se sale antes de los primeros 15 s no queda nada (además, la web pide >10 s para mostrar el botón). Verificado con `brs-node` simulando sesión: la request llega al gateway y termina aunque el reproductor ya se cerró; `playStart` = segundos de "Reanudar". Confirmado por el usuario en el Roku real (aparece "Reanudar" al volver y retoma). "Seguir viendo" del Home sigue sin conectar (usa el historial viejo de MiCHV).
    - [x] Paso 3 — controles propios (`PlayerControls`, calcado de `PlayerTopBar.tsx` + `Seekbar.tsx` + `PlayerControls.tsx`/`PlayerControlsLive.tsx`, medidas en px de la web a 1920 — webOS declara `resolution 1920x1080` y los `rem` son de 16px porque el `font-size` va en `body`). Capa negra al 50% sobre el video con fade de 0.3 s; arriba a la izquierda (top 62, left 40) botón redondo "volver" + título (40px, nueva fuente `dmSansBold40` en `FontManager.brs`) y programa (24px, 90%); abajo (padding 55px) la barra de 12px (`#525252`, progreso `#FA6428`, perilla blanca de 30px que con foco pasa a naranja con halo) y los tiempos (`MM:SS` o `HH:MM:SS`); encima, a la derecha (bottom 140, gap 22), botones redondos de 56px (borde blanco al 60%, con foco naranja `#ff3c00` + relleno blanco al 10%): reiniciar, play/pausa y la píldora "Calidad <valor>" con su lista hacia arriba (fondo `#08090C`, borde `#333`, opción enfocada naranja). En vivo (`live_redirect`): sin barra ni reiniciar, botones a bottom 80. Íconos convertidos de los SVG reales (`iconos-reiniciar/fila/config/play/pause/volver`) con `@resvg/resvg-js` (scratchpad `svgconv/`; maneja trazos, a diferencia del parser propio) en `images/player/`, más anillos/rellenos de botón, píldora y barra como 9-patch. Comportamiento: visibles al arrancar y en cada `playing`, se ocultan a los 4 s; con los controles ocultos OK pausa/reanuda y cualquier otra tecla solo los muestra; al aparecer el foco va a la barra (en vivo a play/pausa); barra: izquierda/derecha saltan 15 s y +15 s por cada toque a menos de 400 ms (tope 300 s), aplicado 600 ms después del último, sin que la perilla vuelva atrás mientras el video salta; arriba → botones, abajo → oculta, OK → play/pausa; botones: arriba → volver, abajo → barra; volver: abajo/derecha → botones, OK cierra. Back siempre sale (como la web). Extras de Roku: los botones play/pausa, adelantar y retroceder del control también funcionan. Calidad: se baja el m3u8 maestro (`ParseHlsVariants`/`ResolveHlsUrl` en `PlayerUtils.brs`), "Auto" + una opción por altura (mayor arriba, como los niveles de hls.js); elegir una recarga la lista de esa calidad desde la posición actual (Roku no fija niveles de un HLS ya cargado) y si falla vuelve a Auto. Spinner de la escena mientras el video está en `buffering`. `ClosePlayerPage` ahora muestra el sidebar sobre cualquier página que lo use (no solo la vista de programa). Verificado con `brs-node` + ECP (duración simulada): barra, botones, volver, lista de calidades con las opciones reales del maestro y cambio a 1080, ocultar/mostrar, OK con los controles ocultos. No portado: la franja de "cargado" de la barra (el nodo `Video` no expone el buffer). Pendiente: verlo en el Roku real (en particular la capa al 50% sobre el video: en este equipo la transparencia de un degradado sobre `Video` falló antes).
    - [x] Correcciones del paso 3 reportadas en el Roku real: (1) el botón "volver" mostraba el ícono de pausa y su círculo se veía ovalado, (2) la píldora "Calidad" se veía ovalada y no cubría el texto, (4) "los movimientos de foco no funcionan bien". Causa común: los hijos de cada botón repetían ids (`pRing`/`pIcon`/`pFill`) y **`findNode()` busca desde la raíz del componente, no desde el nodo en que se llama** — todo cambio pensado para play/calidad caía en el primer botón ("volver"): le ponía el ícono de pausa, le estiraba el anillo al ancho de la píldora y le dibujaba el foco siempre a él, mientras la píldora nunca recibía su ancho. Fix: ids únicos por botón y acceso por índice (`getChild(0/1/2)` = relleno/anillo/ícono). Regla: no repetir ids dentro de un mismo componente aunque estén en grupos distintos. (3) Al mantener apretada una flecha en la barra, se detenía y aparecía el spinner: el salto se aplicaba 600 ms después del último toque y, si la repetición del control no llega a ese ritmo, se aplicaba a mitad de camino y el video se ponía a cargar. Fix: mientras la tecla está apretada la perilla avanza sola (`holdTimer` cada 0.2 s tras 0.4 s, mismo salto acelerado) y el salto se aplica recién 600 ms después de soltar (`PlayerPage.onKeyEvent` ahora pasa los `press = false` a `PlayerControls.HandleKeyRelease`; las repeticiones de la tecla mantenida se ignoran). Verificado con `brs-node` + ECP (`keydown`/`keyup`: 100 → 655 s mientras se mantiene, un solo salto al soltar; el foco recorre reiniciar → play → calidad → volver). Confirmado por el usuario en el Roku real: todo funciona bien (controles, foco, barra y calidad).
    - [x] Paso 4 — "A continuación" + panel de episodios (`NextEpisodeOverlay.tsx` + `EpisodeSidebar.tsx` + la lógica de `VideoPlayer.tsx`). **"A continuación":** a 60 s del final, si hay un capítulo siguiente en la lista (la de la categoría invertida, igual que la web), aparece una tarjeta de 32vw (614×235, degradado 135° al 95%, radio 0.6vw) abajo a la derecha (right 4vw; bottom 12vw con controles y 4vw sin ellos, se desliza en 0.3 s) con "A continuación" en naranjo, la imagen 16:9 de 13vw (esquinas con un marco que repite el degradado de la tarjeta, sin `MaskGroup`), "En N segundos" sobre una franja oscura y el título (3 líneas). Va **debajo** de la capa de controles (en la web z-index 100 < 998). Sin controles la tarjeta toma el foco (borde naranjo de 0.2vw + `scale(1.05)`): OK pasa al siguiente; con controles se llega desde los botones o el botón volver con arriba/abajo (arriba → volver, abajo → botones, izquierda/derecha bloqueadas). A los 0 s (o al terminar el video) pasa sola; si se vuelve atrás con la barra a más de 60 s del final, se esconde. **Panel de episodios:** nuevo botón "Episodios" (`iconos-fila`) entre play/pausa y calidad (solo con capítulos y fuera de vivo); abre una capa negra al 50% + panel de 500px a la derecha que entra deslizando, con un item por capítulo (padding 12/16, título en negrita de 1.8rem, naranjo el actual, triángulo de play y fondo blanco al 10% con foco, alto variable medido con `boundingRect()`), centrado en el capítulo actual y con scroll animado que mantiene el enfocado al centro. Arriba/abajo recorren, izquierda cierra todo (panel y controles), derecha vuelve al botón, OK en otro capítulo lo reproduce y en el actual cierra todo; con el panel abierto los controles no se ocultan solos. **Cambio de capítulo:** como el `navigate(ep.link)` de la web, la misma `PlayerPage` recarga con `params = { link, slug, initialSeconds: 0 }` (`PlayChapter`; `OnParamsSet` corta el anterior y resetea tarjeta/panel), así back sigue volviendo a la vista de programa. Assets nuevos en `images/player/` (tarjeta, borde, marco de la imagen, franja de la cuenta, fondo de item 9-patch, triángulo) generados con resvg. Verificado con `brs-node` + ECP (Betty la fea, 5 capítulos, posición simulada a 50 s del final): la tarjeta aparece enfocada, OK pasa al capítulo 96; el panel abre en el capítulo actual y elegir otro (el 94) lo carga; OK en el actual cierra. Confirmado por el usuario en el Roku real (2026-09-24).
    - [x] Paso 5 — anuncio VAST antes del capítulo (`use-ads-policy.ts` + `VastPlayer.tsx` + `deviceAdService.ts`). **Política:** sin `vast_app` no hay anuncio; con `ads_free.default` del feed de configuración (`homeConfig`; **sí viene** — el análisis inicial lo leyó en el nivel equivocado: `default: never`, `free: never`, `payperview: owner`, `subscriber: always`) y el tipo del usuario (`UserData.adsFreeType`, el campo `ads_free` de `/userInfo`, `default` si no hay): `always` = sin anuncio, `never` = con anuncio, `owner` = sin anuncio solo si tiene acceso al contenido restringido; si el feed no trajera `ads_free`, respaldo: con suscripción activa no hay anuncio. La web usa solo el bloque `default` (hay otro `LG` que ignora), igual acá. **URL** (`BuildVastUrl()` en `PlayerUtils.brs`): la misma sesión DPS del video (`forceSessionParams`) + `appendAdParamsToVastUrl` — `&amp;` → `&`, `iu` sin `app.`, `url`/`ref` a 13go.cl, `description_url` si falta, `env=vp`, `correlator` al azar, `vpos=preroll`, `impl=s`, `unviewed_position_start=1`, y el ID publicitario de Roku (`rdid` = RIDA, `is_lat` = `IsRIDADisabled()`, `idtype=rida`) en vez del TIFA/LGUDID. **Reproducción:** nueva `PrerollAdsTask` (Task con `Library "Roku_Ads.brs"`: `setAdUrl` + `getAds()` + `showAds(pods, invalid, página)`), el capítulo espera con el spinner hasta que termina; sin anuncio, con error o si en 15 s no empezó (`adsTimeout`, el timeout de seguridad del VastPlayer) arranca el capítulo; back mientras se pide sale del reproductor, y durante el anuncio las teclas son de RAF (back ahí sale, como la web). Al terminar el foco vuelve a la página. Si la librería RAF no está (el simulador brs-node no la trae) se sigue sin anuncio. Verificado con `brs-node` como invitado: política `default → never`, URL del anuncio idéntica a la de la web con el RIDA, sin anuncio (Google no devolvió uno) → arranca el capítulo. Trampa encontrada: en BrightScript `print "texto" (expr)` interpreta el paréntesis como llamada sobre el string y revienta (`Function Call Operator ( ) attempted on non-function`) — separar con `;` o sin paréntesis. Pendiente: ver un anuncio real en el Roku (solo si Google tiene uno para mostrar); el SIMID/overlays de Broadpeak son de En vivo, no de capítulos. **Corrección (2026-09-24):** la web cambia `sz` 512x288 → 640x480, pero con eso Google responde vacío: las campañas de estos ad units son de 512x288. Medido desde Chile: tag tal cual 32/32 con anuncio, con los ajustes 0/32; probando cada ajuste por separado solo el `sz` lo rompe (`iu` sin `app.`, `env=vp`, `url`/`ref`, sesión DPS y RIDA no afectan). `BuildVastUrl` deja el `sz` del feed (6/6 con anuncio con la URL completa del Roku). Los anuncios son InLine de Google, 30 s, MP4 progresivo 640×360 a 1280×720. (El User-Agent no influye: también llena desde curl/Node.) Confirmado por el usuario en el Roku real (2026-09-24): la publicidad en los capítulos funciona.
    - [x] Paso 6 — GA4 en todas las vistas (decisión del usuario: como el `AnalyticsProvider` de la web pero con la plataforma de Roku; sin Chartbeat). `MainScene` arma el hit de `ga4Service.ts` (`TrackPage({ path, title? })`): POST sin cuerpo a `https://www.google-analytics.com/g/collect` con `v=2`, `tid=G-33D1K2C2YN`, `cid` persistente (32 hex, en la sección de registry `Canal13GoDevice`, no se borra al cerrar sesión), `sid` = ms del arranque del canal, `_s` contador de hits, `en=page_view`, `ep.title`/`dt`, `ep.path`/`dl` = `https://www.13go.cl` + ruta, `ep.device_type=Roku` y `_p` al azar; va por `RunBackgroundTask` → `ContentAPIAction.SendAnalyticsHit` (GA4 responde 204). Sin título, el último tramo de la ruta con guiones como espacios y mayúscula inicial ("/" → "Home"); no repite la misma ruta dos veces seguidas (`lastTrackedRef`). **Automático:** un `Timer` de 0.5 s mira la página de arriba del stack (cubre push, reemplazo y back, el equivalente al cambio de ruta con el debounce de 200 ms de la web) y la mapea a la ruta de la web: Portada `/home`, Programas `/programas`, En vivo `/en-vivo`, Radios `/radio`, Búsqueda `/buscador`, Mi Cuenta `/mi-cuenta`, Editar perfil `/edit-perfil/{id}`, bienvenida `/login`, vinculación `/connect`, "¿Quién anda ahí?" `/whosthere`. **Manual** (la web salta el automático en esas rutas): la vista de programa manda `/programas/{slug}` con el título del programa (o "Programa VOD") al cargar y al volver a ella; el reproductor manda el link del capítulo con su título al cargar cada capítulo. Verificado con `brs-node` + ECP: `/home` al arrancar; programa → capítulo → back al programa, cada uno con su `page_view` y respuesta OK de GA4. Pendiente: confirmarlo en el Tiempo real de GA4 (filtrar por `device_type = Roku`).
  - [x] Splash en video (pedido del usuario: "como funciona en c13_reloaded, un video traído desde una API"), calcado de `SplashView.tsx`: el video `splash_tv` del feed `configuracion` (hoy `splash-tv-2026-v4.mp4`, 7.5 s, H.264 + AAC) a pantalla completa sobre negro; al terminar o si falla, sigue a la app. Nuevo componente `SplashScreen` (`Video` sin UI; se queda con todas las teclas, como en la web no se puede saltar; si no llega a `playing` en 8 s sigue igual). `MainScene` lo arranca apenas vuelve la config (`StartSplash`, encima de todo) y **la sesión se sigue restaurando por detrás**; la primera pantalla espera a que termine (`StartAfterSplash(showPicker)` reemplaza los `StartApp()` de `StartAsGuest` y `OnGetUserInfoAPIResponse`: Portada, o Portada + "¿Quién anda ahí?"); si al terminar la sesión todavía no resolvió, queda el spinner. Solo una vez por apertura del canal y nunca con un deep link (retrasaría el contenido). El splash estático del `manifest` sigue siendo el que Roku muestra mientras carga el canal. Diferencia con la web: allá el video va después del chequeo de sesión; acá corren en paralelo. **Sin usuario va al login** (pedido del usuario, reemplaza la decisión del Paso 4d.4 de entrar como invitado): en la web todo menos login/vinculación/"¿Quién anda ahí?"/suscripción está detrás del `AuthGuard`, que redirige a `/login`. `StartAfterSplash(target)`: `"login"` → `OnboardingPage` como pantalla raíz sin sidebar (back ahí abre el diálogo de salida; `HandleBackKey` ahora trata igual una raíz con el riel oculto), `"picker"` → Portada + "¿Quién anda ahí?", `"home"` → Portada (relanzamiento con perfil guardado). Cerrar sesión también lleva al login (antes volvía a la Portada como invitado), y si el refresh del token falla al arrancar, igual. Verificado con `brs-node` + ECP sin sesión: splash (salió por el límite de 8 s, no reproduce video) → login, GA4 `/login`, back → diálogo de salida. Pendiente: verlo en el Roku real.
  - [x] Home — candado en "Señales en vivo" (pedido del usuario), calcado de `PlaylistItemCarousel.tsx` + `.card.bloqueado .imagen::after`: cada señal del feed trae `restriction`/`packs` y se marca `blocked` con `ValidateRestriction()` (hoy 8 de 13 bloqueadas para un usuario sin suscripción); el círculo muestra el velo negro al 46% y el candado de 2vw (mismos assets que En vivo, `circle_bg.png` teñido + `images/live/lock.png`), dentro del círculo y debajo del anillo. Campo nuevo `blocked` en `ProgramItemNode` (si no, `setFields` lo descarta). Elegir una señal sigue abriendo En vivo (ahí se ve el canal bloqueado).
  - [x] Home — "Seguir viendo" (pedido del usuario), calcado de `SeguirViendoSection.tsx` + `TrackingListCarousel.tsx` + `TrackingItem.tsx` + el filtro de `use-home-data.ts`. Reemplaza el "Seguir viendo" heredado de MiCHV (`fetchAndStoreWatchHistory`, `createLastWatchedSlider`, observers `isWatchHistoryFetched`/`updatedContinueWatchData`), que pedía un endpoint inexistente en 13go. Ahora es una sección más del CMS (`despliegue = "seguirviendo"`, hoy en la posición 3) procesada en orden: con sesión y perfil pide `/accountTracking/{uid}/history` (`AuthAPIAction.GetTracking`), se queda con los `event_type = "progress"` con menos del 95% visto, hasta 10; sin sesión o sin items la fila no se dibuja. Tarjeta nueva `TrackingCardItem` (17vw = 326px, imagen 16:9 con brightness .5 sin foco, esquinas con marco `#08090C` y anillo de foco propios en `images/home/`, duración abajo a la derecha, barra de avance naranja de 6px, programa en naranjo y capítulo en negrita); `SliderView` usa ese componente y sus medidas para `format = "tracking"`, y `HomePageParser` arma esos items como `ContentNode` con `update(item, true)` (los campos del historial no existen en `ProgramItemNode`). Al volver al Home (ej. después de ver un capítulo) se vuelve a pedir el historial: si la fila ya tenía tarjetas se reemplazan sin mover el foco (`updateContent`); si aparece o desaparece, se reacomodan las filas (`RenderHomeRows`, la fila vacía queda armada en su lugar pero no se dibuja). Elegir una tarjeta abre el capítulo desde donde quedó, con la vista de su programa apilada debajo (en la web, back desde el reproductor va a `/programas/{slug}`); la restricción la valida el reproductor (la web tiene la condición de `/suscribe` invertida en `TrackingItem.tsx`, no se copió). Verificado con `brs-node` con sesión e historial simulados: de 3 items (50%, 97% y terminado) queda 1; abrirlo apila el programa y reproduce el capítulo 95 desde el segundo guardado. Pendiente: verlo en el Roku con una sesión real.
  - [x] Home — las tarjetas de "Destacados" (la fila bajo el hero) ya reproducen (pedido del usuario), calcado de `FeaturedItem.tsx` (`handleClick`) + `LiveDirectPlayerView.tsx`. Los items guardan `link`, `type`, la key de rudo (`rudoKey`), `restriction`, `packs`, `vast_app` (`vastUrl`) y `DPSDAIAssetKey` (campos nuevos en `ProgramItemNode`). `HomePage.OpenFeaturedItem`: **capítulo** (link `/programas/{slug}/{cat}/{cap}`) → `OpenChapterLink` (compartida con "Seguir viendo"): reproductor con la vista del programa apilada debajo (back va al programa, como la web); **en vivo** (`type = "live"` o un link que no es `/programas/...`, hoy `/13go-live-c13`) → `ShowPlayerPage({ live: {...} })`: nuevo modo de `PlayerPage` (`StartLiveDirect`) sin cadena de capítulos — valida la restricción como `handleClick` (sin sesión → login; plan que no alcanza → mensaje de suscripción), pide el m3u8 a la media info de rudo con la key, lo firma si es restringido, usa el stream de DAI (`dai.google.com/linear/hls/event/{key}/master.m3u8`, como la web, sin SDK) si trae `DPSDAIAssetKey`, y reproduce en modo en vivo (controles de vivo, sin tracking ni episodios; back al Home); GA4 `/player/live` ("Live"). Verificado con `brs-node` con sesión simulada: la señal en vivo carga su m3u8 y aplica la política de anuncios; el capítulo de Betty la fea apila su programa y reproduce el 97. Pendiente: verlo en el Roku real.
  - [x] Video recortado en los reproductores (reportado por el usuario en el Roku real: en vivo no se veía completo arriba y abajo; "siempre debería verse todo aunque queden bordes negros"). No es del canal: las señales son 16:9 (1280x720 / 1920x1080, verificado en los m3u8) igual que el nodo `Video`, que además no recorta; es el overscan de la TV (el mismo que ya cortaba bordes del sidebar y de las filas). Fix: el video a pantalla completa ahora va dentro del área segura de Roku para FHD, **1728x972 en [96,54]** (90%, mismo 16:9, sin deformar) sobre el fondo negro que ya tenían: `PlayerPage` (capítulos y en vivo directo de Destacados) y `LivePage` a pantalla completa (XML y `hideOverlay()`). Los controles y overlays siguen ocupando toda la pantalla. No se tocó: el reproductor chico de En vivo con el overlay abierto (1248x702 arriba a la derecha, como la web) ni el splash (la web lo muestra con `object-fit: cover`, recorte intencional). Pendiente: confirmarlo en la TV del usuario (si su overscan fuera mayor al 5% por lado, habría que achicar más).
  - [x] En vivo — nombres de programa en la grilla (reportado por el usuario: en Canal 13 todos los bloques decían "Canal 13"). En el EPG (`global_epg.json`) algunas señales (Canal 13, 13 Internacional, Ecuavisa) mandan el nombre del **canal** en `title` y el programa real en `episodeTitle` ("Tu Día", "Teletrece Tarde", "Vale Todo"); en el resto `title` ya es el programa. La web muestra siempre `title` (tiene el mismo problema). Fix (`LivePage.ProgramTitle`): si `title` es igual al campo `channel` del EPG (sin distinguir mayúsculas) o viene vacío, se usa `episodeTitle`; si no, `title`. Afecta la grilla y el "programa actual" de la cabecera. Efecto de paso: en Machos (EPG `channel` = "Machos") ahora se ve "Machos | Capítulo 99". Las señales cuyo `title` es el nombre de una serie (13Go1 "Torneo Arusa", D13, 13Festival) siguen mostrando ese nombre en todos los bloques, como la web (el detalle está en `episodeTitle`). Verificado con `brs-node` contra el EPG real.
  - [ ] Publicidad en las señales en vivo (VAST + DAI) — análisis hecho, sin código. **Decisiones del usuario (2026-09-24):** DAI en las señales con `assetKey`; VAST en todas (habrá señales con VAST y DAI). Relevado en el sitio real **13go.cl** (la base; el usuario inició sesión con una cuenta sin suscripción y otra con suscripción). El sitio no reproduce él mismo: embebe el **reproductor de Rudo** en un iframe (`rudo.video/live/{key_live}?st=event={DPSDAIAssetKey}~exp~hmac&platform=13goweb&ref=13go.cl`; el `st` lo firma el backend de 13go, sin datos del usuario; en premium ese mismo `st` es el `auth-token` del stream). Variables de la página de Rudo por señal: `haveAds` (preroll VAST con IMA, `adsURL` = `rudo.video/ads/vmap/live/{key_live}`), `DAI` + `ASK` (= `assetKey`: al terminar el preroll —o si no hay anuncio, error o timeout— pasa a DAI con el SDK de IMA, `LiveStream('hls', ASK)`), `DV` (1 = URL de Google directa; 2 = reescribe `dai.google.com` a `redirector.dps.live/hls-video-gdai/{slug}` + sesión DPS), `playout` (anuncios VAST cuando el stream marca una pausa comercial), `streamURL` (respaldo: SSAI de DPS `origin.dpsgo.com/ssai/event/{key}`, o HLS normal si no hay key). Medido: El 13 (libre, con key) `haveAds=1 DAI=1 playout=0`; Machos (libre, solo `assetKey`) `haveAds=1 DAI=1 playout=1`; T13 (libre, sin key) `haveAds=1 DAI=0 playout=1`; 13Pop y 13Cultura (premium, con key) `haveAds=1` **`DAI=0`** `playout=1` y stream SSAI con `auth-token`. **Regla observada: DAI solo en señales libres con `assetKey`; en premium no hay DAI, va el stream de DPS con token.** Sin suscripción las premium muestran candado y no abren; con suscripción abren. **La cuenta con suscripción vio el mismo preroll VAST en El 13** (`haveAds` no depende del usuario). El VMAP de Rudo trae un solo preroll hacia Google (`/112372207/app.13go/{canal}/preroll`, `sz=512x288`), que llena 4/4 (la conversión de `c13_reloaded` a `sz=640x480` no llena, 0/4). De paso: en capítulos el sitio usa preroll (pod de 2, máx. 120 s), midrolls cada 630 s y postroll con tags `13go.cl/.../{pre,mid,post}roll` `sz=656x368` (desde curl no llenaron); y la grilla de En vivo del sitio muestra `episodeTitle` en todas las señales. **Decisiones siguientes del usuario:** los suscriptores tienen publicidad en vivo igual que en el sitio (sin exención por `ads_free`), y la grilla muestra `episodeTitle` en todas las señales.
    - [x] Paso 1 — acceso a las señales: `LivePage` marca `blocked` con `ValidateRestriction(restriction, packs)` (antes `restriction <> "0"`, que bloqueaba también a los suscriptores y a las de `restriction` 2); al elegir una señal de suscripción (`restriction` distinta de 0 y 2) con `assetKey` pide `authContent` (type `live`, id = `assetKey`) y reproduce `redirector.dps.live/hls/{tramo tras /hls/}/playlist.m3u8?auth-token={token}` (`BuildLiveTokenUrl`, el `buildLiveUrl` de la app de TV; DPS lo redirige al mismo stream que usa el sitio); sin token o sin `assetKey`, el stream normal (como la web); las respuestas de una señal que ya no está elegida se descartan. Todas las URLs de vivo llevan la sesión DPS (`ForceSessionParams`, `platform=13GOTVRoku`). `LivePage` ahora incluye `PlayerUtils.brs` y usa sus `ParseHlsVariants`/`ResolveHlsUrl` (se borró su parser propio, que chocaba de nombre). Grilla: `ProgramTitle(ev)` = `episodeTitle` si viene, si no `title` (como 13go.cl). Verificado con `brs-node` con sesión simulada: con un plan que incluye las premium ninguna queda bloqueada y 13Pop pide el token (con token falso cae al stream normal); sin plan las 8 premium quedan bloqueadas; URL con token igual a la de la app de TV. Nota: el análisis estático no puede revisar `LivePage.brs` por su BOM (error preexistente). **Bug en el Roku real** (Vecinos al límite: 401 de Google): el token quedó codificado tres veces (`event%25253D...`): el gateway ya lo entrega codificado (`event%3D...`), `BuildLiveTokenUrl` lo volvía a codificar y `GetTextByUrl` (calidades) pasaba la URL por `EncodeUri()`. Fix: si el token trae `%` se decodifica y va tal cual, como el reproductor de 13go.cl (`auth-token=event=...~exp=...~hmac=...`); `ContentAPI__GetTextByUrl` ahora usa `skipEncode` (sus URLs ya vienen armadas). Confirmado por el usuario en el Roku real con la cuenta suscriptora: las premium (Vecinos al límite, 13Pop, 13Cultura, 13 Rec, 13C) se autentican y reproducen sin error; 13Realities (sin `assetKey`) usa el stream normal. Todavía sin comerciales: faltan los pasos 2 y 3.
    - [x] Paso 2 — DAI con el SDK de IMA de Google (`googleima3`) en las señales **libres** (`restriction = "0"`) con `assetKey`: El 13, Machos, 13Go1 y D13 (verificado con curl que Google entrega el stream de las 4). Las de suscripción siguen con el stream de DPS firmado (en Rudo `DAI=0`). `DAIPlayerTask` reescrita sobre el ejemplo oficial de Google (`googleads-ima-roku-dai`, `basic_example/components/Sdk.xml`): `initSdk()`, `CreateLiveStreamRequest(assetKey, "", "")` (como el `LiveStream('hls', ASK)` de Rudo, sin apiKey ni networkCode), espera el `streamManager` hasta 15 s, avisa la URL por `urlData` y queda en un loop reenviando los eventos del `Video` al SDK (mide cada anuncio con los ID3 del stream; RAF para los interactivos). Cambios respecto de la de MiCHV: campo `stop` para terminar el loop (un hilo no se puede matar) y observers con `observeFieldScoped`/`unobserveFieldScoped` (un `unobserveField` normal borraría también los de la página sobre el mismo `Video`); ya no le da el foco al `Video` al terminar una tanda (rompía el foco virtual de las páginas); todo el arranque va en `try/catch` y cualquier falla termina en `errors`. Helpers `NewLiveDaiTask`/`StopLiveDaiTask` en `PlayerUtils.brs`. **En vivo** (`LivePage.startDai`): al elegir la señal arranca la task; con `urlData` reproduce el manifest de Google (solo "Auto": cambiar de variante saca al SDK del stream que mide); con `errors` o si en 20 s no llegó la URL, sigue con el stream normal (el `streamURL` de respaldo de Rudo); se corta al cambiar de señal, ocultar o destruir la página. **Destacados en vivo** (`PlayerPage`): el stream de DAI ya no es la URL cruda de `dai.google.com` sino el SDK, y solo si la señal es libre; el preroll (si corresponde) va antes, como en Rudo. Verificado con `brs-node` (su IMA es un stub que falla en `initSdk`): El 13 entra por DAI, la task falla y la página sigue con el stream normal sin crash, en En vivo y en el reproductor. **Confirmado en el Roku real (2026-09-24), El 13:** el SDK (IMA r.3.75.1) pide el stream (201), la app carga el manifest de Google y se ve un comercial de 30 s completo con todo su tracking (quartiles, complete, Active View, comScore); al terminar vuelve la señal (`termina la tanda`). IMA lo marca como `preroll_ad1`: el propio DAI puede traer un anuncio al entrar, a tener en cuenta en el paso 3 para no mostrar dos seguidos. (El usuario confirmó que ese anuncio salió al entrar a Machos; al volver a El 13 no hubo anuncio de entrada, así que Google no lo pone siempre.) "Solo comerciales sin parar" en El 13 no era un bug: la señal estaba en una tanda propia de 455 s (`#EXT-X-CUE-OUT-CONT ... Duration=455` en el manifest de DAI, segmentos del origen `dpsgo.com`, sin eventos de anuncio en el log = Google no llenó la pausa y pasó la tanda de la TV); al terminar volvió la programación. Los avisos `Failed to create roSGNode with type InnovidDCL/BrightLine` son de RAF buscando sus módulos interactivos antes de cargarlos (no afectan).
    - [x] Paso 3 — anuncio VAST al elegir la señal, en **todas** y para todos (decisión del usuario tras revisar Machos en 13go.cl: `live-jireh.js` de Rudo, con `haveAds = 1` y `DAI = 1`, carga un video de relleno, pide el VMAP y recién al terminar el anuncio —o sin anuncio, error, cancelación o `prerollTimeout` de 10 s— llama `checkTransitionToDAI()`; `playout` solo activa el mismo módulo IMA; en esas señales el sitio también puede mostrar dos anuncios seguidos, el VAST y el de entrada de DAI). `LivePage.selectChannel` ahora corta el preroll/DAI anterior y, si la señal trae `vast` (el VMAP de Rudo para apps, `rudo.video/ads/vmap/live/{slug}?app=true`: un solo preroll a `/112372207/app.13go/{canal}/preroll`, `sz=512x288`), arranca `startPreroll` con la misma `PrerollAdsTask` de los capítulos (RAF resuelve el VMAP tal cual); con `none`/`done` o a los 10 s sin empezar sigue `playChannel` (DAI, firmado o normal, lo que antes hacía `selectChannel`); back durante el anuncio sale de En vivo (`MainScene.HandleBackKey`), y mientras se pide solo back hace algo. Al entrar a En vivo la primera señal también pide su anuncio (como el iframe del sitio). En el reproductor de Destacados en vivo, el anuncio ya no pasa por la política `ads_free` (`ShouldPlayAds`: en vivo siempre, como Rudo). Verificado con `brs-node` + ECP: El 13 → anuncio (el RAF del simulador no devuelve) → DAI → respaldo; cambio a T13 → anuncio → stream normal. **Confirmado en el Roku real (2026-09-24), El 13:** el VAST de Rudo se reprodujo completo (~30 s) y al terminar entró DAI (stream 201, reproducción OK). Avisos `[RAF.err]` esperables, no son errores del canal: `ROKU_ADS_CONTENT_GENRE: no genre set` (un tracker del anunciante usa esa macro y no le fijamos género con `setContentGenre`) y `roUrlTransfer.setURL(...) rejected argument. Salvaged by %-encoding` (URLs de DoubleVerify con macros sin reemplazar como `${GDPR}`, RAF las codifica y las envía igual).
    - [x] Calidad en las señales con DAI (pedido del usuario): en el paso 2 había quedado solo "Auto" por precaución. Ahora `LivePage.onDaiUrl` y `PlayerPage.OnDaiUrl` arman la lista de calidades del manifest de Google como en las demás señales (`fetchQualities`/`OnMasterPlaylistResponse`). Las variantes son de la misma sesión (`.../stream/{stream_id}/variant/.../bandwidth/N.m3u8`: 720p, 480p, 360p, 240p) y el SDK detecta los anuncios por las marcas ID3 de los segmentos, no por la URL cargada, así que elegir una variante no debería cortar la medición. Pendiente: confirmarlo en el Roku (tras cambiar de calidad, en la siguiente tanda tienen que seguir apareciendo `handle event START`/cuartiles de IMA).
  - [x] Avisos en el log del Roku real ("Tried to set nonexistent field loadwidth/loadheight of a Rectangle node", `MonumentalCard.brs` líneas 95-96, repetidos por cada tarjeta del carrusel monumental): `setPosterSize()` fijaba `loadWidth`/`loadHeight` a todo nodo que recibía, pero también se usa con `rCardBg`, que es un `Rectangle` (esos campos solo existen en `Poster`). Eran avisos, no errores (no rompían nada). Fix: solo se fijan si el nodo tiene el campo (`node.hasField("loadWidth")`).
  - [x] Home — títulos de fila con tipografía de marca: `RowList` nativo de Roku no tenía `rowLabelFont`/`rowLabelColor` seteados en `SliderView.brs` (sí lo tenía `ProgramsPage.brs`, ahí se encontró la referencia) — salían con la fuente default del sistema. Se agregó `m.rowList.rowLabelFont = m.fonts.dmSansBold32` / `rowLabelColor = m.theme.white`, acercándose al `titulo-2` de `c13_reloaded` (bold, grande) con el peso más pesado de DMSans que tenemos importado (Bold).
  - [x] Home — fondo: `MainScene.brs` seteaba `backgroundURI = "pkg:/images/other/bg_home.png"` (textura vieja de MiCHV). Primer intento (`m.top.backgroundColor = m.theme.clrPrimary`, el campo nativo de `Scene`) se veía gris en el Roku real, no negro — en Roku ese campo suele pintar solo el letterbox, no el fondo real detrás del contenido. Fix real: activar y colorear el `Rectangle id="rBackground"` a pantalla completa que ya existía en `MainScene.xml` (estaba `visible="false"`, sin usar) con `m.theme.clrPrimary` (`#08090C`, coincide con `--color-3` de `panels_new.css`).
  - [x] Home — margen izquierdo: las filas (`gDetails`/`focusableGroup`) arrancaban en `x=0`, pegadas al riel del sidebar. En `c13_reloaded`, `.main.regular .menu { flex: 0 0 3.125vw; margin-right: var(--padding-viewport) }` — el sidebar colapsado mide `3.125vw` (60px en 1920) + `2.39vw` de margen (46px) = contenido arranca en `x≈106px`. Se actualizaron las dos asignaciones de `m.gDetails.translation` en `HomePage.brs` (con hero y sin hero) de `[0,...]` a `[106,...]`. El `HeroSlider` de arriba queda igual (a pantalla completa, el sidebar flota encima con z-index, no empuja el layout — así es también en la web real).
  - [x] Bug real encontrado probando en un Roku físico (primera vez que se prueba fuera de `brs-node`, vía telnet puerto 8085): al pasar el sidebar de horizontal a vertical, nadie había actualizado la navegación de foco. `MainScene.brs::OnkeyEvent()` seguía usando arriba/abajo para entrar/salir del menú (correcto para el diseño horizontal viejo, no para un riel vertical a la izquierda) — se cambió a izquierda/derecha. Además `TopMenu.brs::onKeyEvent()` interceptaba "derecha" incondicionalmente para saltar al avatar/login (lógica del diseño horizontal, donde el perfil estaba a la derecha del menú) — eso bloqueaba que "derecha" volviera al contenido de la página. Se desactivó ese salto automático (queda pendiente de rediseñar junto con Sidebar 3/3).
  - [x] Bug real #2, mismo dispositivo: el foco ya llegaba al riel pero no expandía (no se veía el texto). `TopMenu.brs::OnFocusChild()` inferír "¿la grilla tiene foco?" leyendo `m.top.focusedChild.id`, un campo con timing indirecto que interactúa mal con la redirección de foco de `RestoreFocus()`/el salto al perfil. Se cambió a preguntarle directo a la grilla (`m.topMenuGrid.hasFocus() OR m.topMenuGrid.isInFocusChain()`), más confiable.
  - [x] Bug real #3 (crash), encontrado con el debugger BrightScript real (telnet): `HomePage.brs::OnGetFeaturedSliderProgramsAPIResponse` — `m.heroSlider.componentHeight = 583` tiraba "Invalid value for left-side of expression" **a pesar de** estar adentro de un `if isValid(m.heroSlider)`. Causa: asignar `m.heroSlider.items = [...]` dispara sincrónicamente `HeroSlider::onItemsSet()`, que mueve el foco al botón "Ver ahora" — eso puede encadenar hasta `initVar()` (que pone `m.heroSlider = invalid`) si hay una navegación fuera del Home ocurriendo casi en simultáneo (condición de carrera). Fix: guardar `m.heroSlider` en una variable local antes de usarlo repetidas veces en el mismo bloque, para no depender de que el campo `m` siga apuntando a lo mismo a mitad de camino.
  - [x] Bug real #4, mismo dispositivo: seleccionar cualquier ítem del sidebar (incluso "Portada" estando ya en el Home) recargaba el Home entero desde cero. Causa real, confirmada con un `print` de diagnóstico: la grilla vertical dispara `itemSelected`/`selectedItem` con el ítem "Portada" apenas el foco entra al riel — sin que el usuario apriete OK — y `MainScene.brs::onTopMenuItemSelected()` no chequeaba si esa página ya era la actual antes de destruir y reconstruir todo (`ShowHomePage(true)` siempre fuerza replace). Fix: comparar contra `m.ViewStackManager.GetTop().id` antes de navegar, y no hacer nada si ya se está en esa página.
  - [x] Hero simple ("destacados") vs. carrusel monumental ("categoria_destacada") en realidad son **dos layouts distintos** en `c13_reloaded`, no el mismo con distinto contenido — se descubrió leyendo `panels_new.css`/`DynamicBanner.tsx` reales (no había forma de verlo bien sin loguearse, así que se instaló Claude in Chrome para poder inspeccionar la web real cuando haga falta). El hero simple (`.destacados-1`) tiene la imagen **chica** (72vw × 41vw) arriba a la derecha, sin degradado, con el texto en una columna aparte a la izquierda (32vw) — nuestro `HeroSlider` hacía lo del carrusel monumental (imagen de fondo completa + degradado + texto encima) para los dos casos. Se le agregó un campo `variant` (`"full"` default = comportamiento monumental sin cambios, `"compact"` = imagen chica arriba a la derecha, con dos degradados hacia `#08090C` — abajo y a la izquierda, generados como PNG con `sharp` ya que Roku no tiene gradientes CSS — y texto a la izquierda con su propio ancho). `HomePage.brs` solo setea `variant="compact"` en el hero de arriba. Pendiente de ajuste fino de medidas una vez visto en pantalla real (`brs-node` no renderiza píxeles).
  - [x] Home — badge "Premium" en tarjetas de `categoria_carrusel`: **no había nada que construir**. Confirmado bajando la imagen real (`raw.image` de `feed/categorias/{id}`) que el badge está grabado en los píxeles del JPG por el CMS (la URL usa el "image style" de Drupal `13go_vertical_premium`) — no es un overlay de la UI. `OnGetHomeCategoryProgramsAPIResponse` ya usa ese campo `image`, así que el badge aparece solo. El único "Premium" real en el código de `c13_reloaded` (`.menu .avatar .premium`) es el anillo de la cuenta premium en el sidebar, sin relación con esto — es parte del Sidebar 3/3, pospuesto.
  - [x] Home — círculos de logo para `senales`/`radios`: nuevo branch `format="circle"` en `CommonItemComponent`, reusando el patrón de `AvatarListItem` (un `MaskGroup` con `profile_mask.png` como máscara circular). Anillo de color (`images/masks/circle_ring.png`, generado con `sharp` a partir de un SVG) teñido vía `blendColor` con el color real de cada canal/radio (`color_principal`/`color` del feed). `PushHomeRow` ganó un parámetro `componentHeight` para que estas filas no usen la altura de tarjeta vertical (576px) sino una más chica (220px). Se agregaron los campos `image`/`ringColor` a `ProgramItemNode.xml` (si no, `setFields()` los descarta en silencio por no estar declarados — mismo tipo de bug que ya nos había pasado antes).
  - [x] Home — número de ranking en `top10`: resultó que ya estaba prácticamente construido y sin usar, heredado de MiCHV — `CommonItemComponent.brs` ya sabía dibujar el número (`number_back.png` + `lNumberRightTop`) cuando `image_orientation="portrait"` y `format="ranking"`, y `HomePageParser.brs::createChildNode()` ya le agregaba el campo `number` (1,2,3...) a cada item cuando la fila tenía ese `format`. Solo hizo falta parametrizar `PushHomeRow(title, items, format = "default")` (antes tenía `"default"` fijo) y pasarle `"ranking"` desde `OnGetHomeTop10APIResponse`.
  - [x] Home — carrusel "monumental" para `categoria_destacada`: se reutilizó `HeroSlider` (el mismo componente del hero de "destacados") en vez de crear uno nuevo, agregándole navegación real entre items (`m.activeIndex` + izquierda/derecha, antes esas teclas no hacían nada) — con 1 solo item el ciclo es un no-op, así que no afecta el uso existente del hero. `HomePage.brs` ahora separa `categoria_carrusel` (tarjetas chicas, sin cambios) de `categoria_destacada` (arma un `HeroSlider` con `imagen_fondo` del feed vía `PushMonumentalRow`). Hubo que corregir la condición de visibilidad en `createDynamicRowList()`/`checkRefreshNodes()` (dos lugares) de `node.id = "heroSlider"` (string fijo, solo matcheaba el hero original) a `node.subtype() = "HeroSlider"` (por tipo de nodo), si no las filas monumentales quedaban armadas pero invisibles. Texto del botón corregido de "Play" a "Ver ahora" (aplica también al hero de destacados).
  - [x] Home — info de texto del banner principal (`DinamicBanner.tsx`/variante "d1" en `c13_reloaded`): agregados `liveLabel` ("EN VIVO", solo si `raw.llamado` viene con dato) y `epigrafeLabel` (nombre del programa, `raw.show`, en naranjo bold) arriba del título en `HeroSlider` — solo para el hero de "destacados" (`OnGetFeaturedSliderProgramsAPIResponse`), no para la fila normal de tarjetas debajo. Bug real encontrado con `brs-node` (ver "Comandos"): crash `i.createDrawFont is not a function` en el arranque, causado por `m.epigrafeLabel.font = m.fonts.dmSansBold24` — **ese tamaño no existe** en `FontManager.brs` (los Bold disponibles son 48/36/32/30/28/23/20/18, sin 24) — leer un campo inexistente de un node genérico da `invalid`, y asignar `invalid` al campo `font` de un `Label` hace que el emulador intente crear la fuente de dibujo igual y explote. Se corrigió a `dmSansBold23` (el tamaño Bold existente más cercano). Lección para el futuro: si se agrega un `m.fonts.dmSansXxxNN` nuevo, verificar primero que ese tamaño exacto esté declarado en `FontManager.brs`, no asumir. Diagnóstico costó ~15 iteraciones de bisección porque el primer intento de descartar "staleness del emulador" con un `run_in_background` dio un falso negativo (el archivo de salida quedó vacío por buffering de stdout, no porque el canal corriera bien) — la forma confiable de correr `brs-cli` para detectar crashes es `timeout N npx brs-cli ... > archivo.log 2>&1` (fuerza flush al terminar), no `run_in_background` sondeado con Read.
  - [x] Home — banner de "destacados" sincronizado con el foco de la fila (no un carrusel propio): el hero simple (`variant="compact"`) en `c13_reloaded` **no tiene botón** ("Ver ahora" solo existe en las variantes d2/d3, el carrusel monumental) y el contenido del banner sigue al ítem con foco en la fila de tarjetas de abajo, no a una navegación propia izquierda/derecha del banner. Cambios: `HeroSlider` ganó un campo `focusedItem` (type="node") — al asignarlo, actualiza el banner (imagen/título/descripción/epigrafe/llamado) vía un nuevo `m.currentItem` (reemplaza las lecturas directas a `m.items[m.activeIndex]` en `setupPosters()`/`updateMeta()`, que ahora solo las usa el ciclo interno izquierda/derecha del carrusel monumental). El botón y su manejo de foco (`setupWatchNowButton`/`SetFocus`/tecla OK) quedaron condicionados a `m.top.variant <> "compact"`. `HomePage.brs::onRowItemFocused` conecta la fila `id="destacados"` a `heroSlider.focusedItem`, y la fila ahora incluye TODOS los items de destacados (antes se borraba el índice 0 para no duplicar con el hero) para que el primer ítem con foco de la fila coincida con lo que el hero ya muestra al cargar. Para que el foco inicial de la página caiga directo en esa fila (no en el hero, que ya no es interactivo): `FocusableGroup.brs::getFirstContentIndex()` — función que ya existía pero estaba sin usar/desconectada — ahora chequea `variant = "compact"` (antes usaba `getHeroSliderIndex()`, cualquier hero) y `onFocusedChild()` la llama en vez de hardcodear el índice 0.
  - [x] Bug real reportado por el usuario probando en el Roku: al subir con las flechas y la fila siguiente era el carrusel monumental (`categoria_destacada`), el foco saltaba visualmente de vuelta al banner de arriba en vez de quedar en el monumental. Causa raíz en `FocusableGroup.brs::slidePanel()`: la animación de scroll vertical decidía "¿este destino es el tope de la página?" preguntando `nextFocusNode.subType() = "HeroSlider"` — válido cuando `HeroSlider` solo existía en el banner de arriba, roto desde que el mismo componente se reusa para el carrusel monumental en medio de la página (ver entrada anterior de "carrusel monumental"). Al entrar a CUALQUIER `HeroSlider` por arriba, forzaba `newY = 0` (scroll a la cima), aunque el nodo lógicamente enfocado fuera el monumental más abajo — el foco técnicamente estaba bien, pero la pantalla se iba al tope, dando la sensación de "vuelve al banner". Mismo patrón de bug (confundir "es HeroSlider" con "es EL hero de arriba") estaba repetido en `setHeroImageHidden`/`getHeroSliderNode()`/`getHeroSliderIndex()`/`showHideSlider()`/`onKeyPressUp/Down()` — el caché `m.heroSliderNode` se sobrescribía con cualquier hero nuevo agregado (`setTranslation()`), así que después de agregar un monumental terminaba apuntando a ESE en vez del banner real. Fix: nuevo helper `isTopHeroNode(node)` (`subType() = "HeroSlider" AND variant = "compact"`) usado en todos esos puntos en vez del chequeo de tipo solo; y en `slidePanel()` específicamente, reemplazado el chequeo de tipo por uno de posición (`nextFocusNode.translation[1] <= 0`, que solo es cierto para el nodo realmente en el tope del scroll) ya que esa función no tiene acceso directo al nodo top-level para usar el helper de variant.
  - [x] Home — información faltante en las tarjetas de "Destacados" (formato landscape/`default` de `CommonItemComponent`): esa rama (`gHorizCard`) solo pintaba la imagen, sin título/epigrafe/duración, aunque los datos ya viajaban en el item. Causa adicional encontrada al revisar el XML: había un `id="lHRightTop"` **duplicado** (uno dentro de la máscara de la imagen, otro suelto al nivel del Group) — `findNode()` siempre resuelve al primero, dejando el segundo muerto/inalcanzable. Fix: el duplicado se separó en dos labels con id propio, `lHEpigrafe` (naranjo bold, sobre el título) y `lHTitle` (blanco), debajo de la imagen calcando `FeaturedItem.tsx`/`panels_new.css` de `c13_reloaded` (`.epigrafe`/`.titulo`); el badge existente `rHRightTop`/`lHRightTop` se reposicionó a la esquina inferior derecha de la imagen (antes en `[0,0]`, sin usar) y se reutiliza para la duración (estilo naranjo/blanco calcado de `rVBottomTime`/`lVBottomTime`, el badge de horario ya existente para tarjetas verticales). Nuevo campo `duration` de punta a punta: `HomePage.brs` (`raw.duration`, confirmado en vivo contra el feed real: formato `"01:11:58"`) → declarado en `ProgramItemNode.xml` (si no, `setFields()` lo descarta en silencio) → `CommonItemComponent.brs`.
  - [x] Home — scroll horizontal "raro" en las tarjetas de Destacados al llegar al borde: bug real de nombre de campo en `SliderView.brs::createDynamicCardsRowList()` — `RowList` (a diferencia de `MarkupGrid`) no tiene un campo `horizFocusAnimationStyle`; el campo real para la animación de foco *dentro* de una fila es `rowFocusAnimationStyle` (`vertFocusAnimationStyle` es *entre* filas). Al asignar un campo inexistente, SceneGraph lo ignora en silencio y la fila quedaba con el estilo de foco por defecto del motor en vez de `floatingFocus` (el mismo patrón que ya usan `ProgramsPage.xml`/`RecommendDialog.xml`, que sí funcionan bien). De paso, corregido `rowLabelOffset` de array plano `[100,15]` a array-de-pares `[[100,15]]` (formato real que usan los RowList que sí declaran esto en XML).
  - [x] Regresión introducida por el fix anterior: al "limpiar" en el mismo commit un `itemSize=[1920,1416]` que parecía dead code (`rowItemSize` ya lo pisaba, según la investigación), se cambió a `[320,180]` — pero `itemSize` en `RowList` es el **viewport visible** de la fila, no solo el tamaño de item por defecto. Con `[320,180]` la fila solo mostraba UNA tarjeta a la vez (cambiando de imagen al mover izquierda/derecha, en vez de deslizar el carrusel completo). Revertido a `[1920,1416]` — la lección es no "limpiar" valores que parecen muertos sin poder probar el efecto real en hardware.
  - [x] Última tarjeta de una fila cortada a la mitad al enfocarla — reportado primero en Destacados (solo 5 items, fácil de llegar al final) pero el mecanismo aplicaría a CUALQUIER fila. Razonamiento: `RowList` usa `focusxOffset=[100]` para fijar la tarjeta enfocada en esa posición X mientras hay contenido después; al llegar a la última tarjeta (nada más que mostrar), en cambio alinea esa tarjeta contra el borde derecho de su viewport declarado (`itemSize[0]`, `1920` = ancho de pantalla completo) — pero la fila real está corrida 106px a la derecha (`HomePage.brs::m.gDetails.translation`, el margen del sidebar), así que ese borde derecho "de 1920" cae 106px más allá del borde real de la pantalla, cortando la última tarjeta esa cantidad. Fix aplicado: `itemSize` en `SliderView.brs::createDynamicCardsRowList()` (rama no-evento) de `[1920,1416]` a `[1814,1416]` (1920-106), tocando solo el ancho — el alto se dejó igual a propósito porque la vez anterior reducir AMBOS valores a la vez rompió el paginado horizontal (ver entrada anterior). Confirmado por el usuario en el Roku real.
  - [x] Rediseño completo del carrusel "monumental" (`categoria_destacada`): el layout anterior (reusaba la variante "full" de `HeroSlider`, pensada como copia genérica del hero simple — imagen de fondo 100% + overlay oscuro completo) **no correspondía al diseño real**. Se encontró que `categoria_destacada` en `c13_reloaded` NO usa `DynamicBanner`/`HeroSection` en absoluto — usa un componente totalmente distinto, `CategoriaDestacadaSection` → `MonumentalCarousel`/`MonumentalCarouselItem.tsx`, con su propio CSS (`carrusel-monumental` en `panels_new.css`). Diseño real: imagen a la derecha ocupando solo 83% del ancho (no pantalla completa), degradado horizontal (negro→transparente) solo sobre esa franja de imagen (no un overlay oscuro parejo), bloque de texto angosto (max-width 40vw) a la izquierda sin epigrafe/llamado (solo título + bajada + botón "Ver ahora", en ese orden), tarjeta de alto fijo ~33vw, y un título de sección (`titulo-2`) arriba del carrusel que antes no existía en absoluto en Roku. Cambios: nueva variante `variant="monumental"` en `HeroSlider` (rama propia en `setupPosters()`, reutiliza el poster `pFadeLeft` para el degradado horizontal en vez del overlay completo, sin `pFadeBottom`); nuevo campo `sectionTitle`/label `lSectionTitle` para el título de fila (`PushMonumentalRow` en `HomePage.brs` ahora lo setea); reordenados `descLabel`/`bWatchNow` en el XML de `lgDetails` (antes el botón quedaba ANTES de la descripción en el stack vertical, al revés del orden real título→bajada→botón — bug preexistente heredado de cuando se creó la variante "full", nunca notado hasta comparar contra el componente real). Sin poder confirmar visualmente fuera del Roku real.
  - [x] Ajuste fino tras feedback del usuario en el Roku ("no se ve igual: falta foco, título, fondo del título, alto fijo, botón distinto"): 1) el "header" con el fondo `soporte.png` en `panels_new.css` es `position:absolute` **superpuesto** sobre la esquina superior izquierda de la tarjeta, no una fila propia que empuja el contenido hacia abajo — el `headerHeight=70` del primer intento estaba mal, se sacó por completo y la tarjeta ahora arranca en y=0. 2) Nuevo `pTitleBg` (Poster con la URL real `soporte.png`, descargada para confirmar su tamaño nativo 497×256 y escalarla a 576×297 ≈ 30vw) con `lSectionTitle` como hijo, agregado al final del XML (mayor z-order, por encima de la imagen) — antes el label estaba primero en el árbol y por ende TAPADO por `pImageGroup`. 3) Nuevo `pFocusBorder` (reusa el 9-patch `R5T3_35px_outborder_nopadding.9.png`, ya usado en otras partes del código para anillos de foco) teñido `focPrimary`, visible solo cuando `m.top.hasFocus()` (variant monumental) — el borde naranjo de foco que pedía el CSS (`.monumental-layout.focus::after`). 4) Nuevo `rCardBg` (Rectangle `#08090C` opaco) cubriendo toda la tarjeta, para que el "alto fijo" sea visualmente un bloque sólido y no dependa de que el fondo de la página se note igual. 5) Botón "Ver ahora" rediseñado: la real es un pill **sin relleno** (`.btn` en CSS: `border: 0.21vw solid var(--color-4)`, fondo transparente, borde pasa a naranjo en foco) — antes usábamos el estilo de botón lleno (`filled_r6.9.png`, blanco→naranjo). Cambiado a `isFilledBgOnFocus=false` + `R5T3_35px_outborder_nopadding.9.png` como fondo en ambos estados (gris `clrSecondaryText` sin foco, `focPrimary` con foco), texto blanco siempre.
  - [x] Bug real encontrado en el ajuste anterior: el bloque de texto (`lgDetails`) arrancaba en y=170, pero `pTitleBg` (el ribbon del título) mide 297px de alto — se superponían, tapando parte del título/bajada del programa. Además el contenido (título+bajada+botón) probablemente excedía el alto fijo de la tarjeta (`cardHeight`), sacando texto/botón "fuera del encuadre" como reportó el usuario. Fix: `cardHeight` de `w*0.33` a `w*0.375` (más margen) y el texto ahora arranca en y=310 (debajo del ribbon, no superpuesto). También reforzado que el borde de foco (`pFocusBorder`) no se pierda al navegar entre programas del carrusel (izquierda/derecha): `setupPosters()` ahora re-asegura `pFocusBorder.visible = m.top.hasFocus()` en cada llamada (se ejecuta en cada cambio de item), en vez de depender solo de `onFocusedChild()`.
  - [x] El fix anterior (y=310, cardHeight=0.375w) **tampoco alcanzó** — foto real del usuario en el Roku mostró el botón "Ver ahora" cortado justo en el borde de foco (la línea naranja atravesaba el botón), con "Señales en vivo" (la fila siguiente) apareciendo casi pegado debajo. Conclusión: seguir adivinando offsets en píxeles a mano no es confiable (métricas reales de fuente/line-height en el Roku no coinciden con la estimación). Cambio de enfoque: nueva función `finalizeMonumentalLayout()` que corre DESPUÉS de `updateMeta()` (cuando el texto real ya está seteado) y mide el alto real con `m.lgDetails.boundingRect().height` — si el texto+botón exceden el `cardHeight` mínimo (0.375w), la tarjeta CRECE para contenerlos, y `m.top.componentHeight` se actualiza dinámicamente para que `FocusableGroup` ubique bien la fila siguiente. Se llama en los tres lugares donde el contenido cambia: `onItemsSet()`, `onFocusedItemSet()`, y el ciclo interno izquierda/derecha del carrusel. Patrón ya probado en este código (`SetBadge()` en `CommonItemComponent.brs` ya usa `BoundingRect().width` de forma similar).
  - [x] Usuario reportó el carrusel "muy largo y ancho": el `cardHeight` mínimo se había subido a `0.375w` como margen de seguridad antes de tener el crecimiento dinámico — ya con `finalizeMonumentalLayout()` protegiendo contra overflow real, no hace falta ese colchón extra. Vuelto a `0.33w` (los 33vw exactos del CSS real de `carrusel-monumental .card`), tanto en `HeroSlider.brs` (`setupPosters()` y `finalizeMonumentalLayout()`) como en el valor inicial de `HomePage.brs::PushMonumentalRow`. El ancho de imagen/texto (83%/84%/40vw) no se tocó, ya coincidía con el CSS real.
  - [x] Bug real de raíz encontrado tras seguir reportando "muy alto" y "sin márgenes laterales": el `HeroSlider` del hero simple ("Destacados") es un nodo FIJO declarado en `HomePage.xml` como hermano de `gDetails` (translation `[0,0]`, a propósito full-bleed) — pero el `HeroSlider` del monumental se crea dinámicamente (`PushMonumentalRow`) y termina DENTRO de `gContainer`/`focusableGroup`, que sí hereda el corrimiento de `m.gDetails.translation=[106,0]` (el margen del sidebar). Todo el cálculo de `setupPosters()`/`finalizeMonumentalLayout()` seguía usando el ancho completo de pantalla (`w=1920`) sin descontar ese corrimiento, así que la tarjeta se desbordaba 106px por la derecha (fuera de pantalla, invisible) en vez de tener margen simétrico. Fix: nueva variable `uw = w - 106` (ancho útil) usada en TODOS los cálculos proporcionales del monumental (cardHeight, imgWidth, gradWidth, ancho de texto) en vez de `w` directamente — el padding-left interno del texto (115px) no se tocó, es un concepto distinto (padding propio de la tarjeta, no el margen del sidebar). De paso, `descLabel.maxLines` bajado de 3 a 2 solo para monumental (seteado en `setupPosters()`, no en el XML compartido) para que el contenido necesite menos alto por defecto y dependa menos del crecimiento dinámico.
  - [x] Con el texto ya dentro del recuadro, el usuario seguía viendo el monumental "muy alto y ancho" — confirmado que las proporciones YA coincidían exactamente con el CSS real (33vw alto, 83% imagen), así que no era un bug de paridad sino una preferencia de tamaño en TV (lo que se ve bien en una web ancha de escritorio no necesariamente se ve bien en una TV). Preguntado directamente al usuario en vez de seguir adivinando; eligió "bastante más chico (~60% del actual)". Aplicado un factor ~0.6 a `cardHeight` (0.33w→0.2w), `imgWidth` (0.83w→0.5w), `gradWidth` (0.84w→0.51w), ancho de texto (0.4w→0.3w), y el ribbon del título (576×297→350×180, con el texto reacomodado a y=190 en vez de y=310 para calzar con el ribbon más chico). El padding-left del texto (115px) y las fuentes se dejaron igual — son valores fijos, no proporcionales al tamaño de la tarjeta.
  - [x] Ni el 60% alcanzó — segunda foto del usuario mostró el título del programa apareciendo mucho más abajo de lo esperado (y=190 codificado, pero midiendo la foto da ≈350) y la tarjeta terminaba mucho más abajo que el `cardHeight` mínimo (0.2w) — indicando que `finalizeMonumentalLayout()` la sigue agrandando de más. Causa encontrada revisando el código: `m.pLogo` (el Poster del logo, alto fijo `184` en el XML) **nunca se ocultaba con `.visible=false`** en ningún lado — solo se le vaciaba el `.uri` cuando no hay logo (`m.pLogo.uri = ""`). Un Poster visible sin imagen sigue ocupando su alto declarado dentro de un `LayoutGroup` (no es lo mismo que estar oculto) — ese espacio fantasma de 184px explicaría tanto el desplazamiento del texto como el sobre-crecimiento de `boundingRect()` que alimenta el crecimiento dinámico. Fix: `m.pLogo.visible = false` + `height = 0` + `loadHeight = 0` explícitos en la rama monumental de `setupPosters()` (monumental nunca usa logo). Confirmado por el usuario: el alto ya quedó bien.
  - [x] Con el alto ya resuelto, quedaban los márgenes laterales — el usuario confirmó específicamente que "la imagen no llega hasta el borde derecho de la pantalla" (un hueco visible, no algo tapándola ni un mal recorte). Causa: el intento anterior calculaba todo con `uw = w - 106` (restando el margen del sidebar del ancho disponible) para que la tarjeta NO se desbordara por la derecha — pero eso dejaba la tarjeta más angosta que la pantalla real, con un hueco visible antes de llegar al borde derecho, en vez de tocar el borde de verdad. El error conceptual: restar del ancho angosta la tarjeta simétricamente, pero lo que hace falta es un desplazamiento, no un recorte de ancho. Fix (más principled): igual que el hero de "Destacados" logra ser full-bleed siendo un nodo FIJO fuera del contenedor con el corrimiento de 106px (ver hallazgo anterior sobre `HomePage.xml`), ahora las piezas de fondo del monumental (`rCardBg`, `pImage`, `pFadeLeft`, `pFocusBorder`) usan el ancho COMPLETO (`w`, no `uw`) para su tamaño, y se les aplica un contra-desplazamiento `translation.x = -106` para cancelar exactamente el corrimiento heredado de `gDetails`/`focusableGroup` — logrando el mismo full-bleed real de borde a borde que tiene el hero fijo, en vez de angostar el contenido. El texto (`lgDetails`, alineado al margen del sidebar) y el ribbon del título (`pTitleBg`, alineado con el texto) NO se tocan — se quedan en su posición normal, sin el contra-desplazamiento, ya que sí deben respetar el margen visual del sidebar.
  - [x] El fix "full-bleed" anterior estaba mal interpretado: el usuario probó y "se sigue viendo igual" (el cambio de posición no se notó), y al preguntar aclaró: **no** quería full-bleed — quería margen simétrico en AMBOS lados, como el que ya separa el banner principal del sidebar, no que la imagen llegue al borde real de la pantalla. Corregido el cálculo: en vez de restar el margen una sola vez (`uw = w - 106`, que en realidad apuntaba a "cero margen derecho", el objetivo equivocado) o contra-desplazar para full-bleed, ahora se resta el margen DOS VECES (`cw = w - 106*2 = w - 212`) — el margen izquierdo ya lo da gratis el corrimiento externo de `gDetails` (contenido empieza en x local 0 = pantalla 106), y angostar el contenido a `cw=1708` hace que termine en pantalla `106+1708=1814`, dejando exactamente 106px de margen simétrico también a la derecha. Revertido el contra-desplazamiento (`translation.x` vuelve a `0` en `rCardBg`/`pImage`/`pFadeLeft`/`pFocusBorder`), ya no hace falta.
  - [x] "Casi perfecto" (confirmado por el usuario) — solo faltaba más margen a la IZQUIERDA específicamente, para alinear con las tarjetas de la fila de Destacados (que aparentemente arrancan ~100px más adentro que el margen base de 106px del sidebar — probablemente por el `focusxOffset=100`/`rowLabelOffset=[[100,15]]` que ya usa `SliderView.brs` para esas filas), y un poco de separación vertical con el carrusel de arriba. Agregados `leftInset=100` (sumado a la traslación X de `rCardBg`/`pImage`/`pFadeLeft`/`pFocusBorder`/`pTitleBg`/`lgDetails`, y restado del `cw` disponible para mantener el margen derecho igual) y `topInset=20` (sumado a la traslación Y de esos mismos elementos, y sumado a `componentHeight` para que la fila siguiente no se superponga). `cw` pasó de `w-212` a `w-312` (resta también el `leftInset`).
  - [x] Bug real reportado por el usuario: al moverse izquierda/derecha entre los programas del carrusel monumental, el foco visible (borde/color del botón "Ver ahora") se perdía. Causa: `updateMeta()` llama `setupWatchNowButton()` en CADA cambio de item (cada tecla izquierda/derecha), y esa función usa `m.bWatchNow.update({...})` para setear `backgroundColor`/`unfocusTextColor` — campos cuyo `onChange` en `CustomButton.brs` aplica el color INCONDICIONALMENTE (no según el foco real actual), reseteando el botón a su apariencia "sin foco" (gris) cada vez. El `SetFocus(m.bWatchNow)` que se llama justo después no arregla esto porque el nodo YA tenía el foco (no es un evento de foco nuevo, no dispara de nuevo el estilo enfocado de `CustomButton::OnFocusedChild()`). El estilo del botón es estático (nunca cambia con el item), así que no hacía falta reconfigurarlo en cada tecla. Fix: `setupWatchNowButton()` ahora corre una sola vez, guardado con una bandera `m.buttonStyled`.
  - [x] Con eso arreglado, seguía otro bug: al cargar la app y bajar UNA vez hasta el monumental, el borde de foco no aparecía; recién aparecía si se salía y volvía a entrar. Causa: el redireccionamiento de foco hacia `bWatchNow` dependía de que se disparara el observer `focusedChild` del propio `HeroSlider` (`onFocusedChild()`) — pero cuando `FocusableGroup` le da foco al Group por primera vez (`setFocus(nextCompNode)`), el foco puede quedar en el Group mismo sin que ningún hijo suyo lo tenga todavía, y en ese caso el campo `focusedChild` no necesariamente cambia de valor, así que el observer no se dispara de forma confiable. Fix: nueva función pública `setFocusState(focused)` en `HeroSlider` (hace lo mismo que `onFocusedChild()` pero de forma imperativa) llamada EXPLÍCITAMENTE desde `FocusableGroup.brs` (`onKeyPressUp/Down()`, el primer foco en `onFocusedChild()`, y `focusToIndex()`) justo después de cada `setFocus(...)`, en vez de esperar a que el evento reactivo se dispare solo.
  - [x] Home — más espaciado entre filas: `HomePage.brs::Initialize()` seteaba `m.rowSpacing = 62` (usado por `FocusableGroup.brs::setTranslation()` para el gap vertical acumulado entre filas). El CSS real de `c13_reloaded` (`.contenido .componente { margin-bottom: 7vw }`) da ~134px a 1920 de ancho — más del doble. Actualizado `rowSpacing` a `134` para matchear.
  - [x] Home — "Señales en vivo" rediseñado según spec real (`SenalesSection.tsx`/`PlaylistItemCarousel.tsx` de `c13_reloaded`, no `DynamicBanner`): círculo de `10vw` (192px a 1920 de ancho), fondo sólido `#1C1D28` detrás del logo (antes transparente), logo al 65% del diámetro (antes ~80%), anillo de color por canal, sin badge ni texto sobre el círculo (confirmado que el diseño real tampoco los tiene). Bug real encontrado de paso: `PushHomeRow()` hardcodea `catData.image_orientation = "portrait"` para TODAS las filas sin importar el formato — `SliderView.brs::AddSizeFields()` no tenía una rama específica para `format="circle"`, así que caía en la rama "portrait" y dimensionaba el `RowList` para tarjetas de 324×576 aunque el círculo real dibujado fuera de 200×200, dejando espacio vacío de sobra. Se agregó una rama `format="circle"` explícita (chequeada ANTES que `image_orientation`) con `rowItemSize=[192,192]`. Alcance acordado con el usuario: solo vista + navegación básica — seleccionar una señal ahora llama `MainScene::ShowLivePage()` (recién expuesta en la interfaz XML de `MainScene`, ya existía la función pero no era invocable desde afuera) en vez de no hacer nada, pero SIN reproducir el canal específico todavía (eso sigue en standby junto con el resto de auth/login). Radios no se tocó (sigue sin acción al seleccionar), ya que el pedido era específicamente sobre señales.
  - [x] Bug real reportado por el usuario: "no es la misma imagen de c13_reloaded" en el círculo de señales. Causa: `OnGetHomeSenalesAPIResponse` usaba `raw.imagen` — pero bajando el feed real (`feed/senales.json`) se confirmó que ese campo es una FOTO rectangular de contenido (ej. `13_0_1.jpg`), no el logo del canal. El campo real que usa `c13_reloaded` para esto es `logo_blanco`/`logo_invertido` (`mapToSignalItem()`: `item.logo_blanco || item.logo_invertido || item.imagen`) — confirmado en el TSX real, no adivinado. Fix: misma prioridad en `HomePage.brs`. Único cuidado agregado: el canal "T13 envivo" trae sus logos en `.svg` (Roku no puede renderizar SVG) — se agregó un chequeo de extensión que cae de vuelta a `imagen` (un JPG, sí renderiza) si el logo elegido termina en `.svg`, confirmado bajando el feed completo (de 13 señales, solo T13 tiene logos SVG, el resto son PNG).
  - [x] Bug real reportado por el usuario: detrás de cada círculo de señal se veía una silueta de "cuarto de circunferencia" gris. Causa probable: el `Rectangle` (`rCircleBg`, agregado para el fondo oscuro del círculo) vivía DENTRO del `MaskGroup` junto al logo — pero a diferencia de `Poster` (que sí se enmascaraba correctamente, ya funcionaba así de antes con el logo), un `Rectangle` relleno de color parece no enmascararse igual, quedando como cuadrado sin recortar; las esquinas de ese cuadrado (176×176) sobresaliendo del agujero circular del anillo (`pCircleRing`, 192×192) son geométricamente justo la forma de un "cuarto de círculo" en cada esquina — coincide con lo reportado. Fix: en vez de depender de que el `MaskGroup` recorte un `Rectangle`, se generó un PNG circular sólido pre-renderizado (`images/masks/circle_bg.png`, círculo `#1C1D28` de 176px vía `sharp`, mismo patrón que otros assets de este proyecto) usado como `Poster` FUERA del `MaskGroup` (los `Poster` sí son fiables enmascarando/recortando en este motor).
  - [x] Foco redondo en señales/radios, calcando el comportamiento real: el usuario pidió revisar cómo maneja el foco `c13_reloaded` — el CSS real (`.carrusel-senales .card:hover/.focus/:focus .imagen::before { border: 0.4vw solid var(--color-borde) }`, ya investigado antes) confirma que NO hay un rectángulo de foco separado — el mismo anillo de color del canal simplemente se engrosa (de 2px por defecto a 0.4vw ≈ 8px con foco/hover). Cambios: dos assets nuevos generados con `sharp` con geometría exacta y verificada visualmente (`circle_ring_thin.png`/`circle_ring_thick.png`, mismo círculo de 192px, solo cambia el grosor del trazo 3px/8px, blanco sobre transparente para teñir con `blendColor` como ya se hacía), swap de uno a otro en `CommonItemComponent.brs::setSize()` (hook de foco que ya existía en el código pero estaba vacío/sin usar, heredado de MiCHV). De paso, `SliderView.brs` ahora desactiva `drawFocusFeedback` (el rectángulo de foco nativo naranjo de `RowList`) específicamente para filas `format="circle"`, ya que competía visualmente con el anillo — las demás filas lo mantienen sin cambios.
  - [x] Usuario reportó (y confirmó con foto) que en REALIDAD el problema de "última tarjeta cortada" afecta a TODOS los carruseles, no solo señales — el fix anterior (`itemSize[0] = w - 106 = 1814`, apuntando exactamente al borde teórico de 1920px) no daba margen suficiente en la práctica. La foto mostró la última tarjeta de una fila portrait normal ("Realities") notoriamente más angosta que sus hermanas. Sospecha: overscan real del televisor — un TV físico puede recortar un poco los bordes incluso cuando la señal es "1920px completo", algo que ni el emulador ni un monitor de PC normalmente tienen. En vez de seguir apuntando al límite exacto (que ya demostró no ser confiable dos veces), se le agregó un colchón de seguridad más generoso: `itemSize[0]` de `1814` a `1760` (46px extra de margen). Pendiente de confirmación del usuario — si esto tampoco alcanza, el colchón puede agrandarse más.
  - [x] Bug real reportado por el usuario, relacionado con el anterior: al enfocar un círculo, se veía "aplanado" un segmento pequeño en los bordes laterales/superior/inferior — justo los 4 puntos cardinales donde un círculo inscripto toca el borde de su celda cuadrada. Causa: `rowFocusAnimationStyle="floatingFocus"` agranda ligeramente el ítem enfocado, pero nuestro anillo ocupa el 100% de su celda (192×192) sin margen — al agrandarse, se recorta justo en esos 4 puntos donde ya tocaba el borde. Se revisó el CSS real de nuevo (`.carrusel-senales .card:hover/.focus`) y confirmó que tampoco hay ningún `transform`/escalado al enfocar ahí — solo el engrosado del anillo (ya implementado). Fix: `rowFocusAnimationStyle` condicional en `SliderView.brs` — `"fixedFocus"` (sin escalado) para filas `format="circle"`, `"floatingFocus"` sin cambios para el resto.
  - [x] Home — se sacó el botón "Iniciar Sesión" (pedido del usuario): se eliminó el `CustomButton id="loginButton"` de `TopMenu.xml` y todas sus referencias en `TopMenu.brs` (`SetControls`, `ChangeProfileOnLogin`, `Initlization`, `SetFocusOnLoginMyProfile`, `onKeyEvent`). Sin login, cuando no hay sesión el perfil (`gMyProfile`) queda oculto y no hay nada en la esquina superior derecha; `SetFocusOnLoginMyProfile` ahora sale temprano si no hay sesión (antes mandaba el foco al botón). El `HeroSlider` de arriba y la fila de "Destacados" ahora se comportan como una sola unidad: `SliderView` ganó un campo `keepHeroVisible` (seteado en la fila `id="destacados"` de `HomePage.brs`) y `FocusableGroup.brs` un helper nuevo `keepsHeroImageVisible(node)` (hero compacto + fila con ese flag), usado en `showHideSlider`/`onFocusedChild`/`onKeyPressUp/Down` — así la imagen del hero queda visible siempre que el foco esté en el hero o en las cards de Destacados (antes se ocultaba al bajar desde el hero a esa fila, dejando solo el texto). Verificado con `brs-node`.
  - [x] Ajuste de scroll tras feedback del usuario: (1) al volver subiendo a Destacados el scroll no llegaba hasta arriba del todo, y (2) estando en Destacados, apretar "arriba" movía la pantalla hacia el hero (no debía, porque la fila de Destacados es el límite superior del Home). Causa: `FocusableGroup.brs::slidePanel()` centraba verticalmente TODA fila enfocada (incluida la primera), y `onKeyPressUp()` permitía bajar de índice hasta el hero (índice 0). Fix: `slidePanel()` ahora recibe `isTopContent` y, para la primera fila de contenido (`<= getFirstContentIndex()`), ancla el scroll a `newY = 0` (tope real) en vez de centrarla; `onKeyPressUp()` corta con `if m.focusComponentIndex <= getFirstContentIndex() then return false`, así no se puede subir al hero (que ya es no interactivo). El `back` sigue subiendo al hero (`focusToHeroSlider`) sin cambios. Verificado con `brs-node`.
  - [x] Home — faltaba el título de sección en las filas de círculos (Señales en vivo / Radios): la web (`SenalesSection.tsx`) renderiza `<h2 class="titulo-2">{titulo}</h2>` arriba del carrusel, pero en Roku las filas `format="circle"` usan el carrusel propio (`SliderView.brs::createCircleCarousel()`, no el `RowList` con `showRowLabel`), que no dibujaba ningún título. Fix: nuevo `Label id="lCircleTitle"` en `SliderView.xml` (x=100, `dmSansBold32` blanco, mismo estilo que los row labels), seteado en `createCircleCarousel()` desde `m.top.category.title`; cuando hay título el contenido de círculos se corre 50px hacia abajo (`gCircleClip.translation`/`clippingRect`) y `PushHomeRow(..., "circle", ...)` en `HomePage.brs` pasó de `componentHeight=220` a `270` para hacerle lugar (cubre señales y radios). Verificado con `brs-node`.
  - [x] Home — cards de `categoria_carrusel` alineadas al spec real de la web (`CategoriaCarruselSection.tsx` → `.componente.carrusel-horizontal`): la tarjeta web mide `min-width: 11.6vw` × `padding-top: 139%` (≈224×311 a 1920), esquinas `border-radius: 10px`, imagen `object-fit: cover` y `filter: brightness(.6)` sin foco / `brightness(1)` con foco, separación `margin-right: 1.3vw` (≈25px). En Roku la tarjeta vertical (`CommonItemComponent.gVerticalCard`) medía 324×576 (9:16), con `scaleToFit` y sin atenuación. Cambios: nueva máscara `images/card/card_mask_224_311.png` (rect 224×311, radio 10, anti-aliased, generada con `System.Drawing` ya que no hay `sharp` instalado) y placeholder `card_224_311.png`; `gVerticalCard` a 224×311 con `scaleToZoom`; `CommonItemComponent.brs` atenúa `pVCard.blendColor` a `#999999` sin foco y lo sube a blanco con foco (hook `setSize()`, que antes solo hacía el anillo de círculos); `SliderView.brs::AddSizeFields()` ahora separa portrait default (categoria_carrusel → `rowItemSize [224,311]`, `rowHeights 361`, spacing 25) de portrait `format="ranking"`/event (siguen en 324×576); y `HomePage.brs` pasa `componentHeight=361` al `PushHomeRow` de `categoria_carrusel` (antes usaba el default 626). El borde de foco nativo (`R8_T3_50PX_border.9.png`, ~3px naranjo redondeado) se dejó igual, ya es cercano al `0.21vw` (~4px) de la web. Verificado con `brs-node` (sin errores de compilación).
  - [x] Vista "Programas" (VOD) alineada a `c13_reloaded/src/pages/VOD/VODView.tsx`: la web arma un header "On Demand" (`.programas-cabecera`, `min-height: 29.17vw` ≈ 560px) con la imagen `config.fondo_corporativo` a `85vw` arriba a la derecha + degradados hacia `#08090C`, título `titulo-1` (2.4vw ≈ 46px) y bajada, y debajo un `CategoriaCarruselSection` por categoría (mismo carrusel de 224×311 ya alineado en el punto anterior). La página Roku usaba un `RowList` genérico con tarjetas portrait 324×576 (que con el cambio de tamaño anterior quedaban fuera de su celda `[[320,180]]`), mostraba un `ShortDetailsView` a pantalla completa que cambiaba el fondo al enfocar (no existe en la web) y no tenía header. Cambios: se sacó el `ShortDetailsView` (del XML y de `RlsItems_RowItemFocused`); se agregó `gVodHeader` (Poster `pHeaderBg` 1632×918 con `fondo_corporativo` + `pHeaderFadeBottom`/`pHeaderFadeLeft` reusando los degradados del hero + `LayoutGroup` con título "On Demand" `dmSansBold48` y bajada `dmSansBold23`); el `RowList` pasó a `rowItemSize=[[224,311]]`, `rowHeights=[361]`, `rowItemSpacing=[[25,100]]`, `rowLabelOffset=[[100,15]]`, `focusxOffset=[100]`, `itemSize=[1760,520]`, `translation=[106,560]` y `rowLabelFont=dmSansBold32` (antes `dmSansMedium26`); y `setupHeader()` setea el texto y la imagen desde `m.global.homeConfig.fondo_corporativo`, más `m.scene.hasTopMenuBackground=false`. Pendiente de confirmación visual en Roku (el header queda fijo, no scrollea como en la web).
  - [x] Vista "Programas" — el header ahora scrollea con el contenido (pedido del usuario: "no debe estar fijo, igual a c13_reloaded"): la implementación anterior (header fijo + `RowList`) se reemplazó por el patrón del Home (`FocusableGroup` + `SliderView` por categoría), con el header como primer hijo del `gContainer` que scrollea. Nuevo componente `VodHeader` (`componentHeight`=560, `backgroundImage`, `title`, `description`) con la imagen `fondo_corporativo` + degradados + textos. `ProgramsPage.brs` se reescribió: `setupHeader()` crea el `VodHeader` y lo agrega con `setTranslation`; `OnProgramsResult` arma un `SliderView` por categoría (todas las cards, sin el límite de 10 de `HomePageParser`) vía `buildCategoryContent()`; se eliminó la paginación MiCHV y el `ShortDetailsView`. `FocusableGroup` se generalizó: `getFirstContentIndex()` ahora salta cualquier nodo que no sea fila focusable (`isFocusableContentNode()` = `SliderView`/`MonumentalCard`/`HeroSlider` no-compact) en vez de solo el hero compacto — así el foco inicial cae en la primera categoría y "arriba" no entra al header; y `setHeroImageHidden()` solo toca `hasTopMenuBackground` si hay hero (la vista de Programas no lo tiene, no debe oscurecer su header). Verificado con `brs-node` forzando el arranque en `ProgramsPage` (18 categorías, filas armadas, sin errores) y el arranque normal del Home.
  - [x] Bug reportado por el usuario en la vista de Programas: faltaban los nombres de categoría y el margen horizontal de carruseles/textos. Causa 1: `buildCategoryContent()` no seteaba `rowNode.title` (el `RowList` saca la etiqueta de fila del `TITLE` del row node) → títulos vacíos. Causa 2: el `FocusableGroup` estaba en `[0,0]`; el `SliderView` sí aporta su inset vía `focusXOffset` (verificado con un `print` de diagnóstico: el campo real del `RowList` es `focusXOffset`, el código lo setea en minúscula `focusxOffset` y aun así se aplica, valor 100), pero el Home lo compensa con su `gDetails` en `x=106` y Programas no tenía ese contenedor. Fix: `FocusableGroup` de `ProgramsPage` a `translation=[106,0]` (igual que el Home → tarjetas y títulos de categoría en `x≈206`) y `buildCategoryContent(title, items)` setea `rowNode.title = title`. El `VodHeader` ahora vive dentro de ese contenedor corrido 106, así que su fondo full-bleed se contra-desplaza `-106` (`pHeaderBg` en `[182,0]`, fades en `[-106,...]`) y el texto queda alineado con las filas (`translation=[100,200]`). Verificado con `brs-node` (arranque forzado en ProgramsPage, 18 categorías, sin errores).
  - [x] Ajuste pedido por el usuario: las cards de Programas quedaban muy abajo al renderizar (el header de 560px + el `rowSpacing` de 134 dejaba la primera fila en y≈694, pegada al borde inferior). Bajado el `componentHeight` del `VodHeader` de 560 a 420 (el texto del header queda centrado igual porque está en y=200); la imagen de fondo sigue sangrando por detrás de la primera fila. Valor fácil de subir/bajar si hace falta.
  - [x] Bug reportado por el usuario: al renderizar la vista de Programas, el foco no caía en la primera card de la primera categoría. Dos causas encadenadas: (1) el `VodHeader` se agrega en `init` (sincrónico) y la página recibe foco antes de que lleguen los datos, así que `FocusableGroup.onFocusedChild` enfoca el índice 0 (el header); cuando después `manageFocus()` vuelve a llamar `SetFocus(grupo)` el grupo ya tiene foco y el observer `focusedChild` no se dispara → el foco quedaba en el header. Fix: `manageFocus()` ahora llama también `focusToFirstRow()` explícitamente después del `SetFocus`. (2) Aun con el índice correcto, el `SliderView` no mandaba el foco a su `RowList`: su observer `onFocusedChild` no se dispara cuando `FocusableGroup` le da foco al Group (mismo bug ya resuelto en `HeroSlider`/`MonumentalCard`, ver Paso 5). Fix: nuevo `SliderView.setFocusState(focused)` (extraído de `onFocusedChild`, enfoca el `RowList`/aplica foco de círculos) expuesto en la interfaz XML y llamado desde `FocusableGroup::setNodeFocusState()` para `SliderView` (antes solo `HeroSlider`/`MonumentalCard`). Verificado con `brs-node` forzando el arranque en ProgramsPage: `firstContent=1`, `focusIndex=1`, `rowList.hasFocus=true`.
  - [x] Bug reportado por el usuario: no se distinguía si un ítem del sidebar estaba en foco, seleccionado o sin ninguno. Causa de raíz: los PNG de `images/icons/sidebar/*.png` tenían el relleno gris `#8C8C8C` (heredado del `fill="#8C8C8C"` de los SVG de la web) — `blendColor` multiplica, así que **solo puede oscurecer/teñir**, nunca aclarar: "foco" (blanco) dejaba el ícono gris igual, "seleccionado" (naranjo) lo volvía un naranjo oscuro casi invisible. Fix: se regeneraron los 5 PNG a blanco preservando el alfa (script `System.Drawing`), así `blendColor` los tiñe de verdad. Además: `MenuContentItem` ganó un fondo de foco `pFocusBg` (`R5Filled_30px.9.png` blanco a `opacity=0.22`, redondeado) que se muestra solo con foco y se adapta al ancho (256×80 expandido / 76×76 colapsado), y la prioridad de color cambió a seleccionado→naranjo (independiente del foco), con foco→blanco, ninguno→gris `#9b9b9b` (el de la web). Resultado: 4 combinaciones distinguibles (foco+sel = naranjo + fondo; foco solo = blanco + fondo; sel solo = naranjo; ninguno = gris). Verificado con `brs-node` (compila sin errores).
  - [x] Revisión del foco del sidebar (pedido del usuario): la navegación `contenido → sidebar` se hace dejando que "izquierda" burbujee hasta `MainScene` (`OnkeyEvent` → `SetFocus(m.TopMenu)`), pero tres carruseles la consumían en el primer ítem y nunca dejaban salir al riel. La web siempre hace `setFocus('SIDEBAR')` en el primer ítem (`ProgramItem.tsx`, `PlaylistItemCarousel.tsx`, `MonumentalCarouselItem.tsx`). Fixes: `SliderView.brs::onKeyEvent` (carrusel de círculos/señales-radios) ahora devuelve `false` en `circleFocusIndex = 0` para "izquierda" (y en el último para "derecha"); `MonumentalCard.brs::onKeyEvent` ya no hace wrap con `MOD` — en el primer ítem "izquierda" burbujea al sidebar y en el último "derecha" no hace nada (mientras anima sigue consumiendo); `HeroSlider.brs::onKeyEvent` igual (sin wrap; como el hero compacto tiene 1 ítem, "izquierda" sale al sidebar). Los `RowList` (categorías, destacados, top10, seguir viendo) ya burbujeaban bien en el borde. Verificado con `brs-node` (sin errores de compilación); pendiente de prueba en Roku real (el emulador no simula la tecla).
  - [x] Vista "Radios" (calcada de `c13_reloaded/src/pages/Radio/RadioView.tsx` + `components/RadioPlayer/RadioPlayer.tsx` + `pages/Radio/components/RadioCard.tsx` + CSS `.main.radio`): nueva `RadioPage` con (1) reproductor arriba — fondo `radio-fondo.jpg` full-bleed (`60vh`, base `#060608`, anclado arriba-derecha vía `loadStatus`) y tarjeta blanca centrada `35vw×10vw` con el logo de la radio sobre un cuadrado del color real del feed, "EN VIVO" + nombre y play/pause; (2) título "Radios" centrado (`titulo-2` → `dmSansBold32`); (3) grilla centrada de tarjetas cuadradas `10vw` (nuevo `RadioCardItem`): fondo del color de la radio, logo encima, borde `3px→6px` con colores ciclados `#ff3c00/#ff0000/#fa6428/#587fed`, atenuado `brightness(.45)` sin foco (overlay negro `opacity .55`) y a full con foco. Assets nuevos generados con `System.Drawing` en `images/radio/` (fondos/bordes redondeados radio 10 y fondo del player) + `radio_fondo.jpg` bajado del CDN real. Navegación calcada de la web: foco inicial en el player, OK reproduce/pausa, "abajo" salta a la tarjeta actual, "izquierda" en la primera tarjeta sale al sidebar, "arriba" vuelve al player, OK en una tarjeta cambia la radio (autoplay) y devuelve el foco al player. Reproducción con nodo `Audio` (HLS `liveUrl` de cada radio, autoplay al entrar, se corta al salir/destruir la página). Datos vía `ContentAPIAction::GetJsonByUrl` → `apiEndPoints.GetRadios` (`cdn.rudo.video/assets/canal-13/playlists/static/radios.json`). Integrado en `MainScene` (`ShowRadioPage`/`GetRadioPageObject`, el ítem "Radios" del sidebar ya navega, `isDestroy`). Verificado con `brs-node` forzando el arranque en `RadioPage` (compila, fetch real de las 4 radios, sin errores); pendiente de prueba en Roku real (audio y layout).
  - [x] Bug reportado por el usuario: en la vista de Radios las flechas no movían el foco. Causa de raíz encontrada con `brs-node` + el servidor ECP (`brs-cli -e` + `POST /keypress/:key`), que permitió simular teclas de verdad: el foco se movía a `gPlayer`/a las tarjetas con `setFocus(true)`, pero esos `Group` tienen `focusable=false` por defecto y el `RadioPage` quedaba fuera del *focus chain* (`isInFocusChain()=false`), así que su `onKeyEvent` nunca se llamaba. Fix: `RadioPage` mantiene el foco en su propio nodo raíz (que sí es focusable) y maneja todo con foco "virtual" manual (`m.focusArea` = `player`/`cards` + `itemHasFocus` en las tarjetas + `pPlayerFocusBorder`), igual que el carrusel de círculos. Además, `setFocusToPlayer`/`setFocusToCards` ya no llaman a `onFocusedChild` (evitaba un loop que reseteaba el área a `player` en cada tecla); `onFocusedChild` ahora solo aplica/limpia visuales según el área actual, y `onKeyEvent` re-aplica los visuales al inicio. Verificado con ECP: `down` (player→tarjeta 0), `right`/`left` (mueve entre tarjetas), `up` (vuelve al player), `OK` (play/pause), sin errores.
  - [x] Bug reportado por el usuario: los logos de las tarjetas de Radios se veían pixelados. Causa: los logos del CDN (`cdn.rudo.video/.../playlists/images/logo_*.png`) son imágenes **1921×1081** y el `Poster` las cargaba a resolución nativa para downscalearlas en el dispositivo a ~192px (la guía oficial de Roku desaconseja explícitamente esto: "avoid loading very large images... load images identical to, or close to, the intended target screen dimensions"). El primer intento (`loadWidth="192"` en el `Poster`) no alcanzó. Solución definitiva: se descargaron los 4 logos reales y se **redimensionaron con `System.Drawing` (HighQualityBicubic) al tamaño exacto de display (192×108, 16:9)** en `images/radio/logo_*.png` (mismos nombres que el feed); `RadioPage.brs::ResolveRadioImage()` mapea la URL del feed a `pkg:/images/radio/<archivo>` si existe (`roFileSystem.Exists`), con fallback al URL remoto si aparece una radio nueva sin asset local. Se sacó el `loadWidth` (ya no hace falta: el asset es del tamaño justo → 1:1, cero escalado en el dispositivo) y el logo pasó a ocupar el ancho completo de la tarjeta (192, igual al `img { width:100% }` de la web). Radios bundleados: `13c Radio`, `SonarFM`, `PlayFM`, `T13Radio`. Compila y corre con `brs-node` (verificado también que los 4 assets locales se resuelven, `exists=true`); pendiente de confirmación visual en Roku real.
  - [x] Vista "Búsqueda" rehecha según `c13_reloaded` (`src/pages/Search/SearchView.tsx` + `VirtualKeyboard.tsx` + `ResultsList.tsx` + `ResultItem.tsx` + CSS `.buscador` de `panels_new.css`): la `SearchPage` de MiCHV (label placeholder + `MiniKeyboard` nativo de Roku + grilla paginada contra un endpoint REST) se reemplazó por la versión real de la web. Izquierda: input tipo pill (bg `#08090C`, borde `#666`) que muestra "Buscar..." o el término tipeado, y un teclado de 6 columnas × 7 filas (`SearchKey`) con el layout exacto de la web (`a..f`/`g..l`/`m ñ o..q`/`r..w`/`x y z 1 2 3`/`4..9`/`0` + `Espacio` span 3 + borrar span 2), tecla normal `#101114` con texto `#ddd`, con foco blanca y texto negro. Derecha: grilla de resultados de 4 columnas (`MarkupGrid` custom `SearchResultsGrid`, item `SearchResultItem`): cards verticales 314×417 (radio 10, `image` del feed con `scaleToZoom`/cover, atenuada sin foco + borde naranjo `focPrimary` con foco; esquinas redondeadas con un `Poster` "frame" encima porque el `MaskGroup` de Roku mapeaba mal el mask), con placeholders "Escribe algo para buscar..." / "No se encontraron resultados"+"Prueba con otras palabras clave.". Datos: se baja **todo** el feed `feed/search` una vez (`apiEndPoints.GetSearch`, 162 programas) y se filtra local por título (`Instr` case-insensitive, tope 50), igual que la web (que filtra `allPrograms` en memoria), sin endpoint de búsqueda por query. Foco: inicial en la tecla 'a'; el teclado usa foco virtual sobre `gKeyboard` (`focusable="true"`, necesario: la raíz de la página no puede recuperar el foco desde la grilla); "derecha" en la última columna pasa a la grilla (si hay resultados), "izquierda" en la columna 0 de la grilla vuelve al teclado (vía `SearchResultsGrid.exitToKeyboard`), "izquierda" en la primera columna del teclado sale al sidebar; OK en una tecla escribe (DEL borra, Espacio agrega espacio) y OK en un resultado abre el detalle (`showDetailPage`). Assets nuevos en `images/search/`; endpoint `GetSearch` agregado a `Global.brs`; `SearchPage` integrado en el `isDestroy` de `MainScene`. Verificado con `brs-node` + ECP y confirmado en Roku real por el usuario. Nota: seleccionar un resultado llama a `showDetailPage`, que sigue pendiente de migración (4d) como en el resto de la app.
  - [x] Vista "En vivo" rehecha según `c13_reloaded` (`src/pages/Live/LiveView.tsx` + `components/LiveGrid.tsx`/`LiveGridRow.tsx`/`LiveItemRow.tsx` + CSS `.main.senal-en-vivo`): la `LivePage` de MiCHV (grilla EPG `CustomMarkupGrid`) se reemplazó por el "zapping" real. Estado inicial: **video a pantalla completa** (`Video` node) del canal seleccionado (autoplay, `preview_m3u8`, HLS); al apretar cualquier tecla aparece un **overlay** con (1) cabecera arriba-izquierda — pill rojo "EN VIVO" + nombre del canal (`titulo-1` → `dmSansBold48`) + programa actual — y (2) abajo la **grilla de canales** (13 activos): cada fila = logo circular con anillo del color del canal + hasta 5 cajas de programa (título + hora local, o punto rojo si está al aire; primera caja blanca, resto gris); el video se achica arriba-derecha (`65%×36.56vw`). El overlay se **oculta solo a los 10s** (Timer). Foco virtual sobre el nodo raíz de la página (mismo patrón que Radios/Búsqueda): izq/der dentro de la fila, arriba/abajo entre canales, "arriba" en la primera fila va al botón de pausa, "izquierda" en la primera caja sale al sidebar; OK en un canal cambia el video y oculta el overlay; OK con el overlay oculto pausa/reanuda. Datos: `playlist_premium.json` (`apiEndPoints.GetPlaylistPremium`) + `global_epg.json` (`GetEPGPrograms`), unidos por `key_live` (canales `active`, eventos con `endTime > ahora`, tope 5); las fechas ISO se parsean a mano (`isoToEpoch`) porque `roDateTime.fromISO8601String` no parsea el offset en algunos equipos/emulador. Assets nuevos en `images/live/` (dots EN VIVO/rojo, candado, 9-patches del item y sus esquinas). Verificado con `brs-node` + ECP: carga 13 canales, arma filas y navega (overlay→filas→controles) sin errores. Pendiente/limitaciones: canales con `restriction != "0"` se muestran **bloqueados** (candado, comportamiento intencional hasta abordar el auth del gateway en 4d.4 — ver nota más abajo); probar en Roku real (video/audio y layout).
  - [x] Serie de bugs de "En vivo" reportados y probados en un Roku real (**modelo 3930X = Roku Express**, gama baja, 1080p, 512MB RAM):
    - Esquinas de `LiveProgramItem` tapaban el borde de foco en el primer/último ítem de cada fila (los 4 `Poster` de esquina, para simular `border-radius`, estaban declarados *después* de `pFocus` en el XML → se dibujaban encima). Fix: reordenar `pFocus` como último hijo.
    - El borde de foco (`item_border_sq.9.png`, 14×14px) era un asset placeholder demasiado chico para estirarse a ~311×117 sin verse como un bloque sólido. Fix: reusar `R5T3_35px_outborder_nopadding.9.png` (43×43, ya probado en los botones de pausa/calidad de esta misma página).
    - Logos de canal (`LiveChannelRow`) pixelados: el `Poster` no tenía `loadWidth`/`loadHeight` (decodifica el PNG remoto a su resolución nativa de 500×500 antes de achicarlo a 82px). Fix: agregar `loadWidth="82" loadHeight="82"`.
    - Candado de canal bloqueado sin el fondo circular oscuro que tiene la web (`.imagen::after`, `#00000075`) detrás — se veía el logo a todo color con el candado encima. Fix: `pLockOverlay` (Poster con `circle_bg.png`, `blendColor="#000000"`, `opacity="0.46"`), sincronizado con `blocked` junto al candado.
    - **Degradado del borde izquierdo del player reducido (`.player::before` en la web, `linear-gradient(90deg, #08090C, transparent)`): no logramos replicarlo en este hardware.** Se probaron 5 técnicas distintas (Poster translúcido con dos gradientes distintos, `MaskGroup` sobre el `Video` con máscara suave y con tramado binario, tramado como Poster con líneas verticales) — todas fallaron o se vieron mal (corte duro, textura de puntos/líneas visible sobre la imagen). Causa confirmada: bug documentado en el foro de Roku ("problem with transparent video overlay") — la transparencia parcial sobre un nodo `Video` no compone bien en la plataforma (contenido 2D **opaco** sí funciona encima del video, como los botones de pausa/calidad). Decisión final: sin degradado, corte recto sin efectos (`pFadeLeft` eliminado de `LivePage.xml`/`.brs`); si se retoma, la única vía viable es un elemento opaco (ej. borde de acento sólido), no transparencia.
    - No se podía mover el foco de Pausa a Calidad: `onKeyEvent` en `focusArea = "controls"` solo manejaba `OK`/`down`, y `updateFocusVisuals()` tenía `pQualFocus.visible = false` hardcodeado. Fix: `m.controlIndex` (0=Pausa, 1=Calidad) + manejo de `left`/`right`.
    - Botón "Calidad" mostraba "Auto" fijo, sin datos reales. Fix: nueva función `ContentAPI__GetTextByUrl`/Task `GetTextByUrl` (trae texto plano, sin forzar `ParseJSON`, para bajar el manifest maestro HLS); `LivePage.brs::parseHlsVariants()` parsea las líneas `#EXT-X-STREAM-INF` (`BANDWIDTH`/`RESOLUTION` vía `roRegex`) y arma la lista real de calidades del canal (ej. 1080p/720p/480p/240p + Auto), ordenada de mayor a menor; `cycleQuality()` recorre esa lista y cambia la URL de reproducción a la variante elegida.
    - Al seleccionar un canal bloqueado, `selectChannel()` igual llamaba a `updateHeader(ch)` antes de chequear `ch.blocked` → el nombre/programa junto al player cambiaba al del canal bloqueado aunque el video siguiera con el canal anterior. Fix: `if ch.blocked then return` antes de tocar el header.
